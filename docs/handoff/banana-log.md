# Banana Log

This log records bananas found and how they were triaged.

## 2026-07-20

| Banana | Triage | Evidence |
| --- | --- | --- |
| A successful product deployment could still omit its own final publication handoff commit | Recorded the READY deployment and smoke evidence, committed/pushed the handoff separately, then explicitly redeployed final `main` instead of relying on an asynchronous Git hook | `24dfaeb`; `dpl_EwYh5Xz7RoCvfSctddY81mJgpizu`; canonical HTTP 200; deployment error/500 queries empty |
| Every selected-product reply appended the same product card, making a follow-up chain repeat identity and position facts under each answer | Used the conversation history already supplied to the resolver to emit the canonical product card only until the same product has appeared once; preserved all text and follow-up actions | focused RED reproduced the second card, then 15/15 GREEN; live Explain -> Risk conversation retained one card and no second Holding-value block |
| `Explain this product` and `Review my performance` had different headings but the explanation still repeated the holding value, return, market price, and the same performance-led card | Added a dedicated Explain matcher and product-definition answer; kept exact allocation claims behind official documents; replaced position metrics with `Structure`, `Currency`, and `Dealing` while reusing the existing logo/stack card | focused RED on the old response, then 14/14 orchestration GREEN; live CZ Future product-detail -> Explain smoke |
| Resolver-only tests could make product follow-up chips look connected while failing to prove the actual UI click path | Added an integrated assistant regression that opens a real conversation, waits for streaming completion, clicks each chip, and asserts the exact product prompt; live browser clicks also verified three different headings | 11/11 assistant tests; 26/26 combined focused tests; live Performance/Risk/Documents click audit |
| Three-column product metrics compressed values/helpers and the generic eyebrow obscured the product identity | Reused canonical `BrandLogo` at `32x32`, removed the selected-product eyebrow, and added an opt-in stacked metric variant while keeping existing portfolio cards unchanged | DOM/CSS regression plus browser computed-size and geometry audit |
| Four selected-product entry topics visually promised four primary routes and made the experience look repetitive | Reduced the initial shelf to `Explain this product` and `Review my performance`, retained deeper risk/checklist help only as contextual follow-ups, and locked different Explain/Performance replies with a regression | focused RED on the stale four-topic mapping, then 14/14 GREEN; browser counts `1/1/0/0` for Explain/Performance/Risk/Considerations |
| Product Detail and the CZ chatbot could drift because they had no selected-security handoff | Passed the canonical `InvestmentCatalogSecurity` snapshot through an optional callback into the existing chat builders, and clear it on Back/unmount | focused RED/GREEN tests; CZ Future browser smoke on `UniCredit Balanced Income Fund` |
| Selected-holding cards navigated back to the page already behind chat, one follow-up navigated instead of answering, and resumed prompts fell back after the selected-screen snapshot disappeared | Removed the redundant rich-card action, made every product follow-up a message scenario, added distinct document/checklist/portfolio replies, and recover the named security from the canonical catalogue before generic matching | four focused RED failures then 12/12 orchestration GREEN; browser chain Performance -> Risk -> Documents -> Portfolio -> Checklist with zero new logs |
| The Buy completion test still expected the quote on Order Data after the reviewed UI moved it to Review Data | Preserved the newer Order Data composition and moved the existing `100,00 EUR` assertion after `Next`, where the canonical quote remains visible | focused Buy-flow test plus fresh full `npm run verify` |
| The accumulated dev log contains older Recharts `ReferenceLine` NaN warnings from the Investments portfolio chart | Kept the unrelated chart outside this task, confirmed the final selected-product/chat interaction added no new warning/error entries, and recorded the historical issue for a chart-specific pass | browser dev-log timestamps precede the final smoke; stack points to `InvestmentPortfolioChart.tsx` |
| PFM category details rendered all monthly/yearly panels and an indicator but exposed only native touch scrolling, so desktop users could not drag to another period | Mirrored the existing Spending mouse/pointer drag contract with threshold, click suppression, nearest-panel snap, listener cleanup, and a focused desktop regression; kept the existing shared timeline as the only data authority | `PfmCategoryDetailScreen.tsx`; `pfm-spending-category-details.test.tsx`; RO Financial browser drag April -> 2026 -> 2025 -> return |
| Filtering a PFM subcategory passed the filtered list back into the chart, so the bubble disappeared and the user could not reactivate it | Kept complete bubble geometry separate from filtered calculations, added pressed/active state and a reversible toggle, and preserved the last-active guard | `PfmCategoryBubbleChart.tsx`; `PfmCategoryDetailScreen.tsx`; focused RED/GREEN test; RO Financial browser smoke |
| Windows renders Unicode country-flag sequences as regional letter glyphs, so the reviewed FX rows showed `RO`/`US`/`GB` instead of flags | Replaced emoji-dependent output with deterministic local SVG artwork for every supported currency and added a focused DOM regression | `CurrencyFlag.tsx`; `payment-shortcuts.test.tsx`; browser screenshot smoke |
| The first FX Amount/Currency mapping used a 24px bold numeric style and a 112px selector column, which made the selector visually oversized | Reused the DS 20px regular numeric style and constrained the selector to a 96px/44px compact control without shrinking its semantic interaction contract | `ExchangeRatesScreen.tsx`; focused 9/9; browser screenshot smoke |
| The previous checkpoint committed 13 reference JPEGs while the asset baseline still expected 159 tracked assets | Re-locked the strict baseline to all 172 tracked assets; did not exclude `To do/`, loosen the audit, or modify image blobs | `scripts/asset-baseline.json`; `npm run audit:assets` |
| Production-reference template identities could leak into the demo dataset | Replaced all names/accounts/amounts with fictional values and asserted the supplied identities are absent | `paymentTemplates.ts`; `payment-shortcuts.test.ts` |
| The initial Financial bubble geometry used absolute coordinates, so four subcategories overlapped and the lower circles were clipped | Replaced the coordinates with a bounded proportional flex/wrap layout and added a regression contract for interactive bubbles | `src/app/components/pfm/PfmCategoryBubbleChart.tsx`; `tests/screens/pfm-spending-category-details.test.tsx`; `npm run verify` |
| Category bubbles initially looked interactive but were exposed as one static image and did not filter totals or transactions | Converted every removable bubble into a semantic button and routed exclusion through `createSpendingCategoryDetail`, keeping hero total, divider total, bubbles, and rows on one filtered detail model | `src/app/screens/analytics/PfmCategoryDetailScreen.tsx`; `src/data/spendingAnalytics.ts`; focused 12/12 tests |
| Adding separate Loan and Mortgage sources could double-count one reused mock profile or make the two bubbles indistinguishable | Assigned deterministic distinct profile IDs and explicit `LOANS`/`MORTGAGE` taxonomy mappings, then asserted both in the same Financial detail | `src/data/accountDetails.ts`; `src/data/pfmCategories.ts`; `tests/data/spending-analytics.test.ts` |
| The supplied `Add Transaction` affordance has no approved transaction-creation flow | Kept it as a presentational shared action and recorded the boundary instead of inventing product behavior | `docs/handoff/known-bananas.md`; `PfmCategoryDetailScreen.tsx` |

## 2026-07-19

| Banana | Triage | Evidence |
| --- | --- | --- |
| Fourteen verified commits existed only on local `main`, so the clean-looking worktree did not mean the work was published | Ran the complete quality gate, pushed all 14 commits through `bb8257d` to `origin/main`, then added this explicit closeout checkpoint | `git rev-list --left-right --count origin/main...HEAD` changed from `0 14`; `npm run verify`; `git push origin main` |
| A stale remote hardening branch made repository state look split even though its work was already unified | Verified that no remote branch was unmerged into `main`, then deleted `origin/codex/product-health-hardening` | `git branch -r --no-merged main`; `git push origin --delete codex/product-health-hardening` |
| A final documentation commit could be left outside the deployed artifact | The closeout commit is pushed first, then that exact `main` state is explicitly deployed to Vercel Production and checked with authenticated smoke | final `origin/main` parity check, Vercel inspection, production access smoke |
| Green GitHub Actions run warned that v4 checkout/setup actions still target the deprecated Node 20 action runtime | Recorded as a dedicated CI-maintenance task; it is non-blocking because the runner forced Node 24 and the complete gate succeeded | GitHub Actions run `29687079235`; `docs/handoff/next-tasks.md` |

## 2026-07-15

| Banana | Triage | Evidence |
| --- | --- | --- |
| Asset baseline failed on a clean Windows checkout because one SVG received automatic LF-to-CRLF conversion | Reproduced to one clean tracked SVG, changed the audit to hash Git-filtered canonical bytes while still including unstaged content, added a regression test, and retained all 159 asset blobs unchanged | `scripts/audit-assets.mjs`, `tests/audits/asset-audit.test.mjs`, `npm run audit:assets`, `git diff 453e7e2..HEAD --` for asset extensions |
| Production access variables were absent after the source fallback was removed | Configured `ACCESS_PASSWORD` and a separately generated `ACCESS_COOKIE_SECRET` as sensitive Vercel Production variables before deployment; `.env.local` remains ignored | `npx vercel env ls`, `.gitignore`, `api/access.js`, `tests/security/access.test.ts` |
| Broad hardening could silently alter images or make rollback impractical | Work ran in an isolated branch with per-direction commits; image path/blob baseline is locked and zero image diffs exist from `453e7e2` | `scripts/asset-baseline.json`, `npm run audit:assets`, `docs/handoff/product-health-revert-map.md` |
| Product-count overrides could leave a removed account/card/investment route selected | Added typed availability normalization to Home plus regression tests and deep-link round-trip coverage for all nine counts | `f3ea71f`, 37 focused tests, full 231-test suite |
| Platform audit still expected the old `Data Snapshot` label | Fixed the source contract to require `Product count overrides` and `Reset to scenario`; rerun passed | `ef5c106`, `npm run audit:platform` |
| Publication state could drift if the final handoff commit were not deployed after the product commit | Final handoff is committed/pushed separately, and its Git-integrated production deployment is verified before closeout | product deployment `dpl_GciexcyciAbjBhUw2dEMwHFcYh9w`, `docs/handoff/current-session.md`, final Vercel deployment check |
| Three reviewed Investments changes appeared after the previous clean checkpoint | Preserved in the requested all-files commit, documented in handoff/capability state, and gated by build/template/platform/diff verification before publication | `git diff`, `npm run build`, `npm run audit:templates`, `npm run audit:platform`, `git diff --check` |
| The requested clean checkpoint combined several already-approved Card Options and HU Kids UX passes in one dirty worktree | Intentionally preserved as one complete local snapshot; no existing product work was discarded or split, and no push/deploy was inferred | `git status --short`, `docs/handoff/current-session.md`, requested local commit on `main` |
| The new HU Kids frost photograph builds to about 2.09 MB | Kept for visual fidelity in this checkpoint and converted into a future optimization task/known banana | `npm run build`, `docs/handoff/known-bananas.md`, `docs/handoff/next-tasks.md` |

## 2026-07-14

| Banana | Triage | Evidence |
| --- | --- | --- |
| A direct Vercel API smoke call created an empty ERROR deployment before the Git-integrated deployment ran | Superseded immediately by the READY production deployment built from commit `9acd88a`; the canonical production alias points to the READY artifact and runtime-error scan is clean | Vercel deployments `dpl_HLrspbWLMuDjQA3py7nwuenD1iJo` (ERROR) and `dpl_ERD6QX3XnWdNy6ojT7AvQuQmuy8G` (READY); `get_runtime_errors` returned no errors |

## 2026-07-06

| Banana | Triage | Evidence |
| --- | --- | --- |
| CZ Chatbot frame overlay initially captured unrelated screen clicks and the right-edge tab could be visually clipped by scrollable screen content | Fixed by mounting the launcher in the phone-frame overlay, setting the overlay wrapper to `pointer-events: none`, restoring `pointer-events: auto` only on the launcher/assistant, and extending the SVG edge shape past the phone edge | `src/app/App.tsx`; `src/app/components/MobileFrame.tsx`; `src/app/components/FramelessDeviceFrame.tsx`; `package/mobile-pi-coapping-chat-package/src/coapping.css`; `npm run build`; `git diff --check`; in-app browser hit-test smoke |
| Normal desktop preview body did not stretch as a flex column, so Level 2 screens such as Account Detail could leave a large empty band below the phone frame | Fixed by making the normal `DemoShell` preview wrapper a flex column so `MobileFrame` receives the full available height and scales the phone larger | `src/app/components/demo/DemoShell.tsx`; in-app browser geometry check confirmed preview container `795px`, phone screen `741px`, balanced top/bottom gaps `27px`; `npm run build`; `npm run audit:templates`; `npm run audit:platform`; `git diff --check` |

## 2026-06-30

| Banana | Triage | Evidence |
| --- | --- | --- |
| Design System Inventory still exposed technical implementation registry blocks that the user explicitly did not want visible | Fixed by removing the visible `Implementation registry`, `App-specific components`, `Generic UI registry`, component search field, and related side-nav/count references from the inventory UI | `src/app/screens/design-system/DesignSystemPage.tsx`; `npm run build`; `npm run audit:templates`; `npm run audit:platform`; `git diff --check` |
| Component implementation knowledge needed to be portable to the separate `Components2`/`all in one` workspace without re-reading Figma | Fixed by creating a dedicated component implementation handoff package, icon SVG catalog, and export script; the package was also copied to `C:\Users\mihai\Desktop\all in one\component-implementation-handoff` | `docs/design-system/component-implementation-handoff/*`; `docs/design-system/platform-icons-svg-catalog.md`; `scripts/export-platform-icon-catalog.mjs`; `node --check scripts/export-platform-icon-catalog.mjs`; catalog sanity check `119` SVG blocks |
| Vite chunk-size warning still appears during production build | Triaged as existing known banana; build succeeds and this is not blocking current GitHub/Vercel publication | `docs/handoff/known-bananas.md`; `npm run build` |

## 2026-06-13

| Banana | Triage | Evidence |
| --- | --- | --- |
| Shared PI `Payments` hero cards became transparent when HU Kids stopped supplying `--pi-payment-hero-bg` | Fixed the invalid inline CSS fallback in `PaymentHeroCard` and added neutral native card bridge variables only inside `HuKidsPiMenuFrame`; verified standalone `Mobile PI` + `Hungary` Payments and HU Kids Aurora Payments on `http://127.0.0.1:4001/` | `src/app/components/payments/PaymentHeroCard.tsx`; `src/app/screens/kids/KidsMarketHomeApp.tsx`; in-app Browser computed styles showed valid native gradients and no console errors; `npm run build`; `npm run audit:templates`; `npm run audit:platform`; `git diff --check` |
| HU Kids theme contrast audit still reflected the old accent-repaint model and printed stale raw-accent failures | Replaced `scripts/audit-hu-theme-contrast.mjs` with a gate for the active runtime contract: native surfaces, subpage wash, payment-card gradient, active nav, progress, and inverse icon/text contrast across light/dark | `node scripts/audit-hu-theme-contrast.mjs` passed on 2026-06-13 |

## 2026-05-27

| Banana | Triage | Evidence |
| --- | --- | --- |
| No repo-level AI operating contract existed | Fixed by adding AI Contributor Operating System docs | `agents.md`, `docs/handoff/*` |
| Workspace was not detected as Git repo, so commit mode could not commit | Fixed by creating `cata009/mobile-banking-cee`, initializing local Git, and setting `origin` | `docs/handoff/work-mode.md`, `https://github.com/cata009/mobile-banking-cee` |
| Existing architecture uses `variant` as a broad concept | Converted into explicit future architecture rule and registry foundation | `docs/handoff/anti-slop.md`, `docs/architecture/PROJECT_MODEL.md` |
| Build produces chunk-size warning from large assets/bundle | Recorded as known banana, not blocking current foundation | `docs/handoff/known-bananas.md`; `npm run build` passed |
| Existing dev server on `6001` showed stale mixed module state during browser check | Started fresh dev server on `5174`; recorded as known banana | Browser verification passed at `http://localhost:5174` |
| Runtime still carried a release-like `variant` field | Fixed by replacing it with explicit `product`, `designSystem`, `baseline`, and `release` state | `src/app/state/demoTypes.ts`, `src/app/state/demoStore.tsx` |
| PI/SME and next design system could not be selected as first-class runtime contexts | Fixed with selectors and planned-state placeholders for unsupported contexts | `src/app/components/demo/DemoTopBar.tsx`, `src/app/components/demo/DemoFeatureSidePanel.tsx`, `src/app/components/UnsupportedContextScreen.tsx` |
| `CountryCode` import in More card config pointed to the wrong module | Fixed by using the official `CountryId` taxonomy alias | `src/app/config/moreCardsConfig.ts`, `src/app/state/demoTypes.ts` |
| No component/screen catalog export existed for future AI training or platform integration | Fixed by adding component registry and AI catalog source export | `src/app/registry/componentRegistry.ts`, `src/app/registry/aiCatalog.ts` |
| `npx tsc --noEmit` cannot run from this workspace | Triaged as known tooling limitation because no local `typescript` package or typecheck script exists | `package.json`, `docs/handoff/known-bananas.md` |
| Initial workspace was not published anywhere | Fixed by creating private GitHub repo and pushing `main` | `https://github.com/cata009/mobile-banking-cee`, commit `2767060` |
| Payments bottom-nav tab did not open a real Payments menu screen | Fixed by adding the Payments screen, country-scoped menu config, navigation wiring, and registry entries | `src/app/screens/payments/PaymentsScreen.tsx`, `src/app/config/paymentsMenuConfig.ts`, `src/app/App.tsx` |
| Products bottom-nav tab did not open a real Products menu screen | Fixed by adding the Products screen, country-scoped menu config, navigation wiring, and registry entries | `src/app/screens/products/ProductsScreen.tsx`, `src/app/config/productsMenuConfig.ts`, `src/app/App.tsx` |
| Products ShopSmart tab rule was not documented in code | Fixed by centralizing tab availability in country-scoped Products config | `src/app/config/productsMenuConfig.ts` |
| Payments `New payment` card was a dead menu item | Fixed by adding a reusable bottom sheet shell, country-scoped New payment sheet config, and card click wiring | `src/app/components/BottomSheet.tsx`, `src/app/config/paymentsMenuConfig.ts`, `src/app/screens/payments/PaymentsScreen.tsx` |
| Products menu cards were inline and did not match the supplied 164x120 / 18px text contract | Fixed by adding a reusable ProductMenuCard component and verifying computed styles in Chrome headless/CDP | `src/app/components/products/ProductMenuCard.tsx`, `src/app/screens/products/ProductsScreen.tsx` |
| Homepage hide amount icon was visual-only | Fixed by adding global amount visibility state, reusable hide/show button, amount masking helper, and Chrome smoke coverage across navigation | `src/app/components/AmountVisibilityButton.tsx`, `src/app/state/demoStore.tsx`, `src/app/utils/amountPrivacy.ts` |
| Account Detail transaction search bar was visual-only | Fixed by turning it into an input, filtering the current account transactions, adding the supplied clear-results icon, and verifying activation scroll/filter/reset behavior in the in-app browser | `src/app/components/accounts/AccountSearchBar.tsx`, `src/app/screens/accounts/AccountDetailScreen.tsx` |
| Dynamic Island had an artificial drop shadow that does not exist on the real device cutout | Fixed by removing the shadow from the Dynamic Island shape and verifying `boxShadow: none` in the in-app browser | `src/app/components/DynamicIsland.tsx` |
| Domestic payment was still only a New payment bottom-sheet menu action | Fixed by adding a mock Domestic payment create/review/sign/success flow and wiring the bottom-sheet Domestic action into it | `src/app/screens/payments/DomesticPaymentFlowScreens.tsx`, `src/app/screens/payments/PaymentsScreen.tsx`, `src/data/paymentFlow.ts` |
| Account Detail transaction rows did not open detail/redo-payment screens | Fixed by making transaction rows clickable, adding Transaction Detail, and wiring `Redo payment` into a prefilled Domestic payment draft | `src/app/components/accounts/AccountTransactionRow.tsx`, `src/app/screens/accounts/AccountDetailScreen.tsx`, `src/app/App.tsx` |
| Reusable UI icons were declared across multiple components as raw SVGs or direct lucide imports | Fixed by adding the centralized `AppIcon` registry, routing reusable app icon consumers through it, and documenting raw-SVG audit boundaries in Design System Inventory | `src/app/components/icons/AppIcon.tsx`, `src/app/screens/design-system/DesignSystemPage.tsx` |
| No automated guard prevents future raw reusable icon SVGs outside the icon registry | Triaged as future automated audit task; manual audit passed for current implementation | `docs/handoff/next-tasks.md`, `npm run build`, `rg -l "<svg" src/app` |
| Products offer carousel only relied on native horizontal overflow and Products vertical gaps were inconsistent | Fixed by adding Account Detail-style drag/snap behavior to the offer carousel and normalizing Products section/grid gaps to `16px` | `src/app/screens/products/ProductsScreen.tsx`; browser spacing/drag smoke passed on `localhost:5175` |
| Analytics bottom-nav tab did not open a real My Spendings screen | Fixed by adding the Analytics screen, navigation wiring, registry entries, and shared-component reuse for header actions, cash banner, transaction divider, and transaction row | `src/app/screens/analytics/AnalyticsScreen.tsx`, `src/app/App.tsx`; build and in-app browser smoke passed on `localhost:5175` |
| Design System Inventory had no color palette view and app colors were scattered as raw values | Fixed by adding `colorRegistry.ts`, a `Colors` tab, Light/Dark theme variables, appearance switches, and app-wide tokenization; raw color audits, build, and browser smoke passed | `src/app/registry/colorRegistry.ts`, `src/styles/theme.css`, `src/app/screens/design-system/DesignSystemPage.tsx`, `src/app/components/demo/DemoTopBar.tsx`, `src/app/components/demo/DemoFeatureSidePanel.tsx` |
| No automated guard prevents future raw reusable colors outside the color registry/theme tokens | Triaged as future automated audit task; manual `rg` audits passed for current implementation | `docs/handoff/next-tasks.md`, `rg -n "#[0-9A-Fa-f]{3,8}" src/app`, `rg -n "rgba\\(|rgb\\([0-9]" src/app src/styles` |
| Products card content could paint above the bottom navigation while scrolling | Fixed by layering the Products scroll content below an explicit `z-20` bottom-nav wrapper; browser verification measured zero overlap and nav elements on top | `src/app/screens/products/ProductsScreen.tsx`; in-app browser verification on `localhost:5175` |
| Screenshot templates were represented as large PNG previews instead of reusable code screens | Fixed for all 30 screenshot templates by adding shared reconstructed JSX previews in `TemplateCodePreviews.tsx`, code-backed registry entries, compact template cards, and a code/source comparison toggle | `src/app/components/templates/TemplateCodePreviews.tsx`, `src/app/registry/templateRegistry.ts`, `src/app/screens/design-system/DesignSystemPage.tsx`, `docs/handoff/next-tasks.md` |

## 2026-05-28

| Banana | Triage | Evidence |
| --- | --- | --- |
| Top-header Messages icons were visual-only or console-only on runtime screens | Fixed by adding the `messages` navigation state, runtime `MessagesScreen`, country-addressable message config, and shared header routing from Home, Analytics, Payments, Products, and More | `src/app/screens/messages/MessagesScreen.tsx`, `src/app/config/messagesConfig.ts`, `src/app/App.tsx`; browser smoke passed on `127.0.0.1:5175` |

## 2026-06-01

| Banana | Triage | Evidence |
| --- | --- | --- |
| Prompt-driven flows had only screenshot-backed templates, leaving common active runtime patterns without explicit code-only template anchors | Fixed by adding 10 code-only templates for Home dashboard, Payments menu, New payment sheet, Products menu, More menu, Contacts directory, Account details info, Messages outbox, Prime advisor, and Prime benefits; Design System Templates now reports 40 code-backed previews: 30 screenshot-backed and 10 code-only | `src/app/components/templates/TemplateCodePreviews.tsx`, `src/app/registry/templateRegistry.ts`, `src/app/screens/design-system/DesignSystemPage.tsx`; build, static coverage, `git diff --check`, and in-app browser smoke passed on `127.0.0.1:5175` |
| Pre-login, panel, Co-Apping, search-result, ShopSmart, Spending category, and confirmation-overlay patterns were still missing explicit code-only anchors | Fixed by adding another 10 code-only templates for Prelogin inactive, Prelogin active, Language selector sheet, Other panel menu, Co-Apping session, Account transactions list, Account search results, Spending money out, Products ShopSmart, and Logout confirmation; Design System Templates now reports 50 code-backed previews: 30 screenshot-backed and 20 code-only | `src/app/components/templates/TemplateCodePreviews.tsx`, `src/app/registry/templateRegistry.ts`, `src/app/registry/componentRegistry.ts`, `src/app/registry/aiCatalog.ts`; build, static coverage, raw color audits, `git diff --check`, and in-app browser smoke passed on 2026-06-01 |
| `AppIcon` still exposed lucide wrappers even when custom SVG equivalents already existed, and RO Kids imported lucide directly | Fixed by removing redundant lucide wrappers, remapping usages to custom registry icons, centralizing RO Kids icons behind `AppIcon`, and documenting the remaining lucide-alone list plus raw-SVG audit boundaries | `src/app/components/icons/AppIcon.tsx`, `src/app/screens/kids/RoKidsApp.tsx`, `src/app/components/templates/TemplateCodePreviews.tsx`; build, icon audits, template coverage, `git diff --check`, and Design System Icons browser smoke passed on 2026-06-01 |

## 2026-06-02

| Banana | Triage | Evidence |
| --- | --- | --- |
| Template-to-app reuse was only descriptive: `relatedComponents` used free-form names and no automated guard verified that templates referenced real components, screens, flows, or code preview IDs | Fixed by turning template metadata into a typed contract with `ComponentId`, `ScreenId`, `FlowId`, screen family, runtime screen, standalone page contract, AI assembly rules, and a project script that audits all 50 templates against the source registries | `src/app/registry/templateRegistry.ts`, `src/app/state/demoTypes.ts`, `src/app/registry/componentRegistry.ts`, `scripts/audit-template-contract.mjs`, `package.json`; `npm run audit:templates` and `npm run build` passed on 2026-06-02 |
| Runtime/templates did not consistently expose mouse pointer affordance for clickable UI, making transaction details and other interactive surfaces ambiguous in desktop demos | Fixed by adding a global interactive cursor policy in `theme.css`, with pointer for native/semantic clickable controls, text cursor for text inputs, and not-allowed for disabled controls | `src/styles/theme.css`; `npm run build`, browser computed-cursor checks, and closeout `git diff --check` passed on 2026-06-02 |
| Bosnia was modeled as one selectable application context even though the operator needs to maintain two Bosnia applications | Fixed by adding `BA_BL` as a separate `Bosnia Banja Luka` country/application variant that intentionally duplicates Bosnia configs, data, translations, registries, and docs while remaining independently selectable | `src/app/state/demoTypes.ts`, `src/app/registry/demoConfig.ts`, `src/app/registry/countryConfig.ts`, `src/app/registry/projectModel.ts`, `src/app/config/*`, `src/data/*`, `docs/handoff/state-of-the-world.md`, `docs/platform-capability-map/README.md`; build, template audit, static `BA_BL` coverage audit, and browser smoke passed on 2026-06-02 |
| BA and BA_BL More/Products/Payments still reflected the previous shared country menu assumptions | Fixed by aligning BA/BA_BL More cards/header actions, Products cards-only behavior, and five-card Payments menus with the supplied market copy while keeping the behavior country-scoped in config | `src/app/config/moreCardsConfig.ts`, `src/app/config/productsMenuConfig.ts`, `src/app/config/paymentsMenuConfig.ts`, `src/app/screens/more/MoreScreen.tsx`, `src/app/screens/products/ProductsScreen.tsx`, `src/app/screens/payments/PaymentsScreen.tsx`; build and in-app browser smoke passed on 2026-06-02 |
| More `Documents` card displayed a hardcoded `12` instead of the actual country-scoped document count | Fixed by deriving the badge count from `documentsConfig.ts` and using the same helper in runtime and Design System specimens | `src/app/config/documentsConfig.ts`, `src/app/screens/more/MoreScreen.tsx`, `src/app/screens/design-system/DesignSystemPage.tsx`; build, template audit, static country/config audit, and in-app browser smoke passed on 2026-06-02 |
| Contacts used a local header/divider treatment, so the title did not collapse into the centered small `PageHeader` title and section separators drifted from the Design System component | Fixed by moving Contacts to shared `PageHeader` scroll progress and shared `SectionHeadingDivider`, with Design System specimen/registry/docs updated | `src/app/screens/contacts/ContactsScreen.tsx`, `src/app/screens/design-system/DesignSystemPage.tsx`, `src/app/registry/componentRegistry.ts`; build, template audit, `git diff --check`, and in-app browser smoke passed on 2026-06-02 |
| No automated regression currently guards the BA/BA_BL country menus, More Documents count, or Contacts header collapse behavior | Triaged as a future automated coverage task; manual build/audit/browser evidence is recorded for this closeout | `docs/handoff/next-tasks.md`, `docs/handoff/current-session.md`; no untriaged blocker remains |

## 2026-06-04

| Banana | Triage | Evidence |
| --- | --- | --- |
| Component-E Build UI Bridge folder was named `ComponentEX`, while docs and `audit:figma-bridge` expect `screenshots/FIgma plugins/Component-E` | Fixed by renaming the folder to `Component-E`, rebuilding the plugin, and rerunning the bridge audit | `npm --prefix "screenshots/FIgma plugins/Component-E" run build`, `npm run audit:figma-bridge` |
| Documents delete actions were present in accessible text even before a row was opened | Fixed by hiding and disabling the row `DELETE` action until the 3-dot menu or swipe-left state reveals it | Browser smoke on `http://127.0.0.1:3005/`: hidden `DELETE` count was `0`, open row count was `1` |
| Documents had no automated regression coverage for newest-first dates, one `NEW`, legal rows, swipe reveal, or legal-delete blocking | Triaged as future automated coverage; local build, template audit, static contract check, and browser smoke evidence are recorded | `docs/handoff/current-session.md`, `docs/platform-capability-map/README.md` |
| Vercel Routing Middleware pass-through broke static/API serving for this Vite deployment | Fixed by removing the middleware and keeping the SPA-mounted access gate plus `/api/access` validation as the active implementation | `src/app/components/security/AccessGate.tsx`, `api/access.js`; build/audits passed |
| Demo access gate has no committed automated regression test | Triaged as a future automated coverage task; manual Node simulations and local browser smoke are recorded for this session | `docs/handoff/next-tasks.md`, `docs/handoff/current-session.md` |
| Demo password has a source-level server fallback | Triaged as an intentional demo fallback plus known banana; before wider external sharing, configure `ACCESS_PASSWORD` and `ACCESS_COOKIE_SECRET` in Vercel and rotate/remove the fallback intentionally | `docs/handoff/known-bananas.md`, `api/access.js`, `middleware.js` |

## 2026-06-05

| Banana | Triage | Evidence |
| --- | --- | --- |
| `.codex-temp/` contained temporary PDF extraction images and dev-server logs and appeared as untracked publish scope | Fixed by adding `.codex-temp/` to `.gitignore` before staging, so only product/runtime source and handoff docs are committed | `.gitignore`, `git ls-files --others --exclude-standard` |

## 2026-06-13

| Banana | Triage | Evidence |
| --- | --- | --- |
| HU Kids appearance selector still risked light/dark ping-pong after the previous fix | Fixed by separating stored appearance preference from effective `themeMode`, and by guarding internal vs external global theme changes in `HuThemeChangePage` | `src/app/screens/kids/KidsMarketHomeApp.tsx`; `npm run build`, `npm run audit:templates`, `npm run audit:platform`, `git diff --check` |
| HU Kids themed Payments/Products/More reused shared PI gradients that double-mixed HU theme neutrals | Fixed with scoped `--pi-*` bridge variables in `HuKidsPiMenuFrame` plus fallback-compatible updates to shared Payments, Products, and More cards | `src/app/screens/kids/KidsMarketHomeApp.tsx`, `src/app/components/payments/PaymentHeroCard.tsx`, `src/app/components/products/ProductMenuCard.tsx`, `src/app/screens/more/cards/*` |
| Automated visual smoke could not complete after the HU theme bridge fix because Browser lost the local tab and timed out, while Chrome headless/CDP was unstable | Triaged as a known banana and next task; build/audits/diff-check passed, but visual HU theme smoke must be rerun from a stable browser session | `docs/handoff/known-bananas.md`, `docs/handoff/next-tasks.md`, `docs/handoff/current-session.md` |
