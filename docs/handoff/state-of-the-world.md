# State Of The World

Last updated: 2026-07-07

## What This Project Is

This project is a standalone `Vite + React` interactive demo platform for UniCredit CEE mobile banking experiences.

Current runtime supports:

- country switching across CEE markets, now including separate `BA` Bosnia and `BA_BL` Bosnia Banja Luka application variants;
- active/inactive app scenario;
- explicit `Baseline` / `Future` state through the stakeholder release control; `Baseline` is the official current truth, while `Future` opens a compatible future-feature dropdown;
- Phase 1 release/baseline operating system with a baseline ledger, feature manifests, legacy R1/R2/R3/R4 registry compatibility, release diffs, promotion readiness checks, and flag-retirement candidates; old R1/R2/R3/R4 preview options are no longer exposed in the stakeholder header;
- Phase 1 project-pack registry covering all 24 product/country combinations across `PI`, `SME`, and `Mobile PI Kids` for `RO`, `CZ`, `SK`, `HU`, `RS`, `BA`, `BA_BL`, and `SI`;
- Phase 1 banking scenario control with mock holdings, entitlements, limits, enabled actions, disabled-action reasons, and a resolved `effectiveAppContext`;
- contract-ready mock banking repositories for accounts, cards, payments, products, entitlements, and scenarios; these still read mocked scenario data today but are shaped for future API adapters;
- Light/Dark appearance switching from the demo top bar, driven by UniCredit design-system theme tokens;
- Light/Dark exact-match token remapping for shared DS colors such as key neutrals, teal actions, status colors, and banner tones, using only approved dark partners from the supplied reference DS table;
- The remaining previously-unmatched active DS colors now also use explicit manual dark-mode pairs supplied during this session, and pure black has been normalized out of the active DS tokens in favor of `#262626`.
- a separated desktop `Flows` menu and full-width Flow Library surface for future flow previews that should not be mixed into the current baseline/country selector. The Flow Library now starts with naked flow search/select controls and local tabs for `Overview`, `Demo`, `Spec`, and `Flow`: Overview carries summary/source/country scope, Demo offers click-through mock preview states, Spec carries longer BA-readable narrative, and Flow preserves connected downloadable journey diagrams. Registered Romania previews now include `RO Round Up`, sourced from RO Enablers node `2344:10093`, with local country chips, English UX spec cards, and connected phone-screen journey diagrams for Entry, create with/without savings account, update, and deactivate scenarios, plus `RO Card PIN`, sourced from RO Enablers node `2247:16744`, with View credit PIN, View debit PIN, Change credit PIN, and Change debit PIN scenarios. They are reachable by deep link with `screen=flow-library&flow=ro-round-up` or `screen=flow-library&flow=ro-card-pin` and remain mock-driven until a future release is approved.
- product switching between `PI`, `SME`, and `Mobile PI Kids`, with an honest planned-state placeholder for SME;
- `Mobile PI Kids` current-design-system runtime is intentionally limited to Slovakia and Hungary. Romania Kids and Serbia Kids runtime apps were deleted on 2026-07-07; RO, RS, CZ, BA, BA_BL, and SI Kids contexts now use the honest planned-state placeholder until a new approved concept is implemented. Slovakia remains the Bulbank document-inspired concept with Products, Education, Tasks, and More pages. Hungary remains the supplied CEE Light Restyle homepage direction for Alexandra, with top daily-spend balance, local request-money/send-money flows, draggable pending-action cards, PI-style hidden amount masks, a single-card `Your cards` panel, HU-local card-details, task confirmation state, HU-owned Saving goals/Learn data in `src/data/huKidsBanking.ts`, shared PI Messages/Contacts/Settings reuse, and the HU-local theme personalization system. Future-design-system Kids contexts still render the honest planned-state placeholder until separate concepts are implemented;
- design-system switching between `current` and `next`, with honest planned-state placeholder for next DS;
- simplified feature side panel for banking scenario, editable product mix, rights, release features, and unplanned flags; duplicate context/release/product/design-system/appearance controls and project-pack evidence intentionally stay out of the drawer. The `Data Snapshot` block now edits the actual mock portfolio counts for accounts, debit cards, credit cards, meal cards, deposits, savings accounts, loans, mortgages, and investments; setting a count to `0` removes that product type/category from the mobile demo, while higher counts generate distinct mock rows.
- generated and static mock card products now order credit cards before debit cards globally across Baseline/Future, products, countries, and scenarios, and the default demo portfolio includes two debit cards. Generated current accounts receive deterministic per-index balances, and generated debit cards link by index to generated current accounts (`Debit Card 1` mirrors `Primary Account 1`, `Debit Card 2` mirrors `Primary Account 2`) instead of every debit card falling back to the first account. Credit cards use `availableCredit` as their displayed product-row/card-category amount, converted into the selected country's currency, so Home and Card Detail `Free To Spend` stay correlated while debit and meal cards continue to mirror their linked current account balance. Card Detail uses the shared Meniga-mapped `Card` component for carousel artwork (`mc-credit-partner-standard` for credit cards, `mc-debit-gold` for the first debit card, and `mc-debit-standard` for the second debit card) and no longer shows the Apple Wallet CTA. The old card SVG artwork assets were removed; Card Component and Flow Library card previews now render through the shared `Card` component instead of image files. The `Card` component uses a single outer rounded mask, container-level background fill, and `preserveAspectRatio="none"` SVG rendering so non-native card slots such as `219x138` do not expose white corner or letterbox artifacts.
- localized translation infrastructure by country;
- More screen title and card labels are now translation-backed across every country/language pair; CZ/SK use the updated English labels `Consent to third parties`, `Digital activity record`, `My applications`, and `Tutorials`, plus local-language equivalents. BA and BA_BL now expose `Tutorials` and `My applications` after `Settings`, Serbia now also exposes `Tutorials`, and their More/Products/Payments headers use the Bosnia-specific `Contact phone` + `Messages` action pair. The Mobile PI `Tutorials` card opens a standard bottom-sheet list with close X and eight tutorial links, then a mock-driven tutorial detail overlay with back/close controls, large visual content, carousel dots, and next/done navigation; Romania uses the BNR exchange tutorial title while other markets use generic currency-exchange copy.
- mobile frame demo shell, including a two-line stakeholder header on actual demo/mobile app screens: row one carries the UniCredit logo, centered `Demo` / `Flows` / `Design system` tabs, profile initials, and logout; row two starts with the combined app/country selector such as `PI - Czech Republic` or `Kids - Hungary` with `App` and `Country` sections in the same dropdown, then carries `Baseline App` / `Future App`, an `Active app` / `Inactive app` scenario dropdown beside `Baseline App` only, the compatible future-feature selector beside `Future App` only, and right-side actions ordered as Refresh, Play / `Open large demo`, Share, and `More actions`. The normal preview body stretches to the full available desktop height so the phone frame scales up cleanly on Level 2 screens without leaving a blank bottom band. Deep-linked Level 2 screens now have deterministic Back fallback parents even when no in-memory navigation stack exists, so direct Account Detail, Documents, Account Info, payment, and investments-history URLs return to their logical parent screens. The `More actions` menu contains Settings, Screenshots, and the current Light/Dark appearance mode; the Play action opens a large focus-preview overlay for the existing demo frame so stakeholders can review the phone surface without the double header taking visual space. When `Future App` is selected, a second dropdown lists only compatible isolated future-feature previews for the current product/country/design-system context and the scenario is normalized to `active`; currently the first item is `CZ - Chatbot` for Mobile PI Czech Republic, including a `32px` black SVG right-edge assistant tab with gradient glow across Home/Level 1 and deeper in-app screens, portal-style open/collapse animation from that tab into the full chat surface, contextual new-conversation entry topics for Level 1 Home, Spending, Payments, Products, and More, contextual help entry from deeper screens, product-aware Account Detail entry topics for current accounts, savings, loans, and mortgages, a color-animated non-teal empty-state AI mark with a time-aware `Teodora` greeting, up to five generic-icon suggested topics per new-conversation state, a compact Chat/For you segment on new-conversation and For you surfaces only, a clean white conversation surface with neutral gray user bubbles, polished mocked assistant replies, contextual message timestamps, contextual pre-reply working states, fast word-by-word streaming before generated replies finalize, rich investment/card/spending response cards with contextual follow-up chips above the composer, and Teodora-addressed contextual Level 2 Help handoffs from Documents, Account Detail, and Card Detail into page-specific new-conversation topics. Platform surfaces such as Flow Library and Design System Inventory intentionally render only the first row because app/country, baseline, scenario, and phone-capture controls belong to the mobile demo context. The stakeholder header uses explicit local SVGs for the Demo and Light/Dark controls so it does not depend on fallback icon-registry behavior. The Screenshots submenu can download PNG captures of either the visible 375x812 phone viewport or an expanded full-height phone screen; the same submenu can copy code-derived Figma-ready `build-ui.screen.v1` JSON layer trees for visible or full-height screen modes, without embedding a full-screen screenshot image; those JSON exports use an unscaled phone clone, app-content background instead of phone-shell black, numeric bounds, Figma paints/effects/text specs, assets, text safety width, designer-friendlier layer names, conservative Auto Layout intent for editable Figma import, app-side generated JSON validation, source metadata, and top-level quality warnings where needed; JSON delivery uses async clipboard writes with `.json` download fallback when browser permissions block copying; full-height exports preserve L1 bottom navigation at the bottom of the exported output when present, and the control is omitted in the Design System Inventory and Flow Library routes where capture is not available;
- CZ Chatbot Level 1 new-conversation entry keeps the internal simulated home-indicator pill hidden while preserving natural bottom spacing; the attempted Gemini-like bottom glow was removed after visual review because it washed the full assistant surface blue.
- CZ Chatbot Home suggested topics now show `How much can I save?`, `Review today's money snapshot`, `What products can I open`, `Review latest 5 transactions`, and `Spot unusual spending`. `How much can I save?` derives a mock monthly saving capacity from the Spending timeline and current-account cash, explains expenses, income, and balances, then offers Saving account at `3.5% p.a.` and Term deposit at `5% p.a.`, amount chips, and a final `Open now` handoff to the selected Product detail page. The final `Ready to open` answer connects the selected amount to the selected rate: Saving account estimates monthly interest, while Term deposit estimates annual interest, with the same rate preview repeated in the presentation card. The monthly-capacity card is placed between the intro copy and the saving-choice card, uses vertical icon rows with right-aligned amounts, and reuses existing colored PFM icons (`Income`, `Shopping`, `Finance`, `Wallet`, `Investments`). Rich-card descriptive text in this savings path uses normal font weight. The two saving product cards are compact presentation-only cards; choosing happens only through the follow-up chips, with `Choose your preferred saving type.` rendered as a connector question under, not inside, the card. The final `Ready to open` product card is also presentation-only; only the lower chips (`Open now`, `Adjust amount`, `Compare products`) act. This savings-capacity path intentionally no longer exposes an `Open Spending` action from the reply card or follow-up chips, and chatbot-visible copy uses `simulation` instead of `demo`. Chat streaming preserves the user's reading position once the user scrolls away from the bottom instead of snapping down at stream completion. `What products can I open` is grounded in the Products `OUR PRODUCTS` shelf config and sheet options, with an `Open Products` handoff; the rich product cards also navigate to Products, select Banking, scroll to `OUR PRODUCTS`, and open the matching product bottom sheet. The transaction replies reuse the transaction-derived Spending timeline/source transaction data, so they can list the latest five transactions with signed amounts and source accounts, then highlight largest recent outgoing movement, heaviest money-out category, and pending items. This remains a mock-driven front-end simulation layer, not a banking analytics backend, regulated savings recommendation, or product-opening execution flow.
- CZ Chatbot conversation navigation now distinguishes list entry from new chat versus selected conversation: the list header shows `OneAI`, uses a right-facing return control instead of close, highlights the active conversation only when opened from detail, and conversation detail exposes `More options` plus a separate top-right close button.
- CZ Chatbot Investments entry now exposes four contextual new-conversation topics on the Investments page: `Start an investment goal`, `Review portfolio context`, `Review my orders`, and `Plan next investment move`. The investment-specific structured replies use the same mock investment portfolio/history builders as the Investments runtime screen, so goal setup, portfolio review, order review, and next-step planning can show rich cards and follow-up chips grounded in portfolio value/performance, holding concentration, currency/asset exposure, and executed/pending/rejected order status. This remains a front-end demo conversation layer and does not execute trades, provide regulated advice, or replace suitability/order authorization flows.
- CZ Chatbot `For you` note: the former static Discovery tab has become a hybrid contextual surface. The first implemented CRM-style demo scenario is Credit Card Detail: a dismissible opportunity card above card transactions can open `For you`, while the floating AI launcher opens clean chat and the `For you` segment button carries the red-dot attention cue when opportunities exist; the Card Detail nudge now uses commercial offer copy (`Upgrade your credit limit to 15 000 CZK`) while keeping `FIND OUT MORE` unchanged; the primary `For you` opportunity is titled `New credit limit for you`, is prioritized from the mock credit-card plafon and proposed plafon (`10 000 CZK` -> `15 000 CZK`) rather than available credit, includes a related card row using the shared Meniga-mapped credit-card artwork/name/masked number, uses a clean non-gradient/non-stroke primary card surface, hides internal CRM trigger copy, uses a centered shared `LinkButton` CTA labeled `I'm interested`, removes weak secondary credit-card cards, shortens the offer body so it fits on one line in the current phone viewport, titles the top offer section `Personalized offers for you`, and organizes supporting content into `Grow your money`, `Next best conversations`, and `Decide with confidence` categories so the management demo reads as contextual offers plus sales-capable conversation starters rather than generic editorial content. `Grow your money` and `Next best conversations` now render pointer drag/swipe-enabled horizontal scroll-snap carousels with additional hidden-behind topics, and `Decide with confidence` shows four guidance topics; Card Detail Help still opens clean support chat topics without sales contamination. Browser/share/PWA metadata now names the platform `Mobile Banking CEE`.
- CZ Chatbot Card Detail / For You credit-limit flow now uses more commercial offer copy, labels the helper as `Your current card limit` instead of `ceiling`, and lets the related credit-card row return to the underlying Card Detail page by closing the assistant. The chat path removes redundant `For Credit Card...` body copy and internal `Open card details` buttons from rich offer cards; final confirmation and acceptance states no longer repeat the offer card. Acceptance is framed as `Signature required`: the customer can accept the offer intent in chat, but the new limit is not active until final terms, strong customer authentication, and user signature are completed. The post-acceptance chip shelf now shows `After acceptance` and `Sign now` instead of `What happens next?`; `After acceptance` leads back to a single `Sign now` chip, and `Sign now` explains the secure signing step without activating the limit from chat.
- QR/device links with `frame=0` render through the frameless real-device shell without the simulated phone StatusBar, Dynamic Island, or themed system-bar wash, so mobile browsers do not show duplicated system chrome. The project also ships PWA/standalone metadata for app-like mobile review, while normal browser tabs remain limited by the browser's own URL and toolbar UI.
- a Vercel-facing demo access gate: `src/app/components/security/AccessGate.tsx` is mounted before the demo app and shows the password page before any runtime banking screen renders. On Vercel, successful password entry is validated by `api/access.js` and represented by signed HTTP-only cookies. `Remember my password` keeps the access cookie for 6 months; unchecked access keeps it for 1 month. Authenticated desktop sessions can also generate short-lived signed QR share tokens for frameless mobile URLs; `AccessGate` consumes `access_token`, sets the normal access cookie on the mobile browser, and removes the token from the URL. Failed attempts are counted in a signed HTTP-only cookie and the 10th failed attempt triggers a temporary 24-hour block with support-only copy. The Vite local dev server uses the same React gate with a local fallback because Vite does not execute Vercel functions.
- PI-like mobile banking screens, including Home, Analytics / My Spendings, Messages, Documents, Account Detail, Account Details, Payments, Products, Investments Portfolio, Prime, More, Settings, and Contacts.
- Investments Portfolio is now reachable from the Home `Investment Portfolio` product card for Mobile PI Retail contexts in every supported country/application variant: `RO`, `CZ`, `SK`, `HU`, `RS`, `BA`, `BA_BL`, and `SI`. The screen uses reusable components for the top tabs, value/performance chart, period chips, Investments action bar, `ALL PRODUCTS` counter divider, sorting chips, active/inactive securities accordions, product cards, fund-suggestion banner, and portfolio distribution chart. The top tab rail now reuses the shared Design System `MessagesMailboxTabs` component in its horizontally scrollable layout, so all five tabs can be selected by swiping. Its `Total value` is calculated from the owned `investment_account` products returned by `useProducts()`, so it follows country currency conversion and amount privacy masking instead of using a static figure. Portfolio-level `Total value`, `Performance`, and chart tooltip values are always displayed in the selected country's local currency, while individual security values may still show their own instrument currency. The portfolio tabs are now interactive: `Performance` shows the existing value chart, while `Product Type`, `Currency`, `Asset Class`, and `Account List` show donut/list distributions grouped from the same mock security holdings. The `History` action opens a mock-driven Investments History flow for the same PI country set, with Transactions/Orders tabs, search, filter panels, active filter summary, transaction/order rows, and reusable detail screens for transaction details and order details. Mock securities can belong to multiple security accounts and instrument currencies, including local-currency, EUR, USD, and GBP accounts.
- More `Documents` card badge now derives from the country-scoped Documents grouped-list config, so it reflects the actual number of document rows for every supported country instead of a hardcoded count. The Documents list helper also normalizes display order for every country: newest groups and newest rows are shown first, with older documents moving downward. Documents rows now model current-ish June 2026 dates, a single newest `NEW` badge, legal/non-legal status, a trailing 3-dot actions control, swipe-left reveal for `DELETE`, deletion confirmation for non-legal files, and a legal-file Info modal that prevents deletion.
- SK/HU Kids concept variants for the active concept pass: Slovakia uses a Bulbank document-inspired Products/Education/Tasks/More experience, while Hungary uses the supplied CEE Light Restyle home composition with Alexandra, HUF balance, supplied quick-action glyphs, local request-money/send-money forms, draggable pending-action cards, a single-card `Your cards` row, local card details, searchable day-divider Kids transaction rows, Face ID card detail reveal, freeze/unfreeze state, PI-style hidden amount masks, task detail/parent-confirmation states, all-money sections, HU-owned Saving goals and Learn modules from `src/data/huKidsBanking.ts`, shared PI Messages/Contacts/Settings reuse, shared PI `BottomNavigation` relabeled to `Saving` and `Learn` where HU needs it, and the local theme personalization boundary.
- Payments `OTHER` / market-specific shortcut headings now use the shared `SectionHeadingDivider` component for title/divider rendering and a horizontally scrollable shortcut rail; each shortcut uses a reusable `PaymentOtherShortcutIconBubble` with 8px padding around a centered 32x32 icon slot, and its label uses the supplied 14px UniCredit bold centered N5 treatment.
- Payments primary hero cards now render through a reusable `PaymentHeroCard` with 9 screenshot-backed artwork variants (`payments1.png` ... `payments9.png`), a 120px card height, 24px title that respects supplied line breaks, 14px subtitle, and dedicated `heroSheets` overlay configuration per card. Romania now has four country-specific primary Payments cards (`Payment to account`, `RoPay`, `Currency exchange`, `Bills & Direct Debit`) and a `SHORTCUTS` rail; BA and BA_BL have five country-specific primary Payments cards and five `OTHER` shortcuts; the remaining non-RO/non-Bosnia countries still use the previous shared baseline.
- Products offer banners now render through a chevron-based reusable card layout with a fixed right image column and clamped title/subtitle copy.
- The Products offer-banner component now supports dropdown-selectable color families with `normal` and `light` tone variants in the Design System inventory.
- Products banking and ShopSmart commercial banners are now country-specific in both copy and color tone, and the offer rails can also vary in banner count by market, so switching country changes the Products storytelling instead of reusing one shared offer set everywhere.
- Products / ShopSmart now uses a separate Figma-mapped `ShopsmartOfferCard` component instead of the old products offer carousel/category grid: the selected tab uses the 48px RO Enablers tab treatment, the content renders an activated-offers summary, `ALL OFFERS` divider, the shared `AccountSearchBar` search/filter strip with clear-search behavior, and a vertical list of 327px ShopSmart offer cards with local optimized Figma images, Active/Activate pills, optional orange tag support, and website/partner metadata icons. The current values are searchable shared mock RO Enablers values until country-specific ShopSmart content arrives.
- Products `OUR PRODUCTS` runtime cards now reuse the standard `ProductMenuCard` artwork support with the supplied product images, while preserving the existing country product card list. Tapping a Products card now opens a standard dimmed bottom sheet across all Mobile PI countries, reusing `BottomSheet` and `NavigationRow`; `Saving and investing` carries the RO Enablers Figma values `Term deposit`, `Saving account`, `Round Up`, and `Mutual funds`, while Romania now has local product-card order and bottom-sheet overrides for Account, Cards, Borrowing, and Insurances and the remaining country option lists stay on temporary shared mock values until country-specific specs are supplied.
- Products `OTHER SOLUTIONS FOR YOU` / `Additional services` is now visible only for Czech Republic and Slovakia; Serbia remains a direct Products page without Banking/ShopSmart tabs. BA, BA_BL, and SI are cards-only Products pages with no offer rail/headings and no Insurance card; BA/BA_BL use `Accounts`, `Cards`, `Loans`, and `Savings`.
- Design System Inventory with `Components`, `Templates`, `Icons`, `Colors`, and `Typography` tabs; each tab has a local search field for its own inventory content, and the sidebar uses a quieter selected state without the old explanatory intro card or submenu separator line. The Overview/Countries surface is intentionally lean: the old coverage-summary cards, country dropdown, Co-Apping badge, currency label, and language/product/more-card detail grid have been removed so the inventory starts from reusable component evidence rather than platform status trivia. `Templates` currently covers all 30 files in `screenshots/` as selectable screenshot-backed templates, adds 20 code-only templates derived from active runtime patterns, and renders all 50 as real JSX code previews with PNG/JPG source comparison only where a source asset exists. Every template now has a typed AI reuse contract with `ComponentId`, `ScreenId`, `FlowId`, screen family, runtime screen links where available, standalone page/state intent, reuse rules, and do-not-invent rules, guarded by `npm run audit:templates`. `Icons` maps reusable app icons through one searchable, ungrouped, compact AppIcon inventory grid with per-icon SVG copy and PNG/SVG download actions; it excludes non-icon control/separator internals such as radio states and divider lines, has removed lucide wrappers that already had custom SVG equivalents, centralizes remaining lucide-alone glyphs behind `AppIcon`, includes the supplied HU Kids quick-action glyphs, uses one canonical share-screen icon entry, and deduplicates chevron-forward, close, and phone variants into `chevron-link`, `close-x`, and `contact-phone` while exposing canonical `chevron-left` and `chevron-up`. The same Icons tab now also exposes a separated `PFM icons` section sourced from the runtime `PFM_CATEGORIES` / `PfmCategoryIcon` mapping, with 23 category cards, SVG copy, and PNG/SVG download support. `Colors` maps `screenshots/Colors.svg` into compact palette swatches, copyable hex values, Light/Dark variants, and matching compact app color map cards without source-token/status badges. `Typography` maps the canonical UniCredit text tokens as a separate searchable inventory.
- Local Figma handoff via `figma-plugins/screen-json-importer`, now named `UniCredit Build UI Bridge`, and the synced `screenshots/FIgma plugins/Component-E` variant named `Component-E Build UI Bridge`: the plugin builds editable Figma layers from `build-ui.screen.v1`, normalizes legacy Build-UI / Component-E-compatible JSON plus inline SVG/PNG/JPEG assets into the same schema, runs preflight diagnostics before creating Figma nodes, shows an English Diagnostics panel with build/extract warnings and companion metadata, and extracts selected Figma frames/components/groups/layers back into the same schema with assets, Figma styles, root frame styling, Auto Layout, text segments, source metadata, warnings, safe wrapping for single-layer selections, `components[]` / `variantSets[]` companions, style refs, component props, bound variables, and optional PNG 2x snapshots. `npm run audit:figma-bridge` provides the repeatable local static, VM smoke, manual smoke fixture import, preflight diagnostics, Diagnostics panel guard, and extracted-JSON re-import round-trip gate for both plugin copies.
- The Design System Inventory `Components` tab now groups multi-variant component families under selector-driven specimens instead of listing each variant as a separate long block, keeping the audit page shorter without changing the components themselves.
- Design System Inventory `Components -> Buttons` now includes a normalized Meniga `Pill` specimen, backed by `src/app/components/ui/Pill.tsx` and registered as `ui.pill`, with primary, secondary, active-counter, loading-counter, and activated variants using the 120x36 / 18px radius / N5 bold / 16px icon Figma contract.
- Design System Inventory `Components -> Forms and controls` now includes a normalized Meniga `CodeField` specimen, backed by `src/app/components/CodeField.tsx` and registered as `ui.code-field`, with filled/error/empty/disabled variants, four 44x64 code slots, 16px gaps, N1 30px bold digits, numeric paste distribution, and accessible error messaging.
- Design System Inventory `Components -> Forms and controls` now maps the Meniga `Navigation` additional cases node `1515:1995` onto the shared `NavigationRow` specimen, with all 18 icon/no-icon/toggle/special cases selectable from one component family.
- Design System Inventory `Components -> Navigation` now maps the Meniga `Divider` node `1058:22303` onto the shared `SectionHeadingDivider` component, preserving the legacy runtime section-divider API while exposing 13 selectable status variants for standard and Light Restyle divider states.
- Design System Inventory `Components -> Cards and content blocks` now maps the Meniga Search `Type=Active remove filters` node `1517:12655` onto the shared `AccountSearchBar`, adding an explicit filters-active state with teal filter icon and `REMOVE FILTERS` action while preserving the default search and filled/clear states.
- Design System Inventory `Components -> Overlays and dialogs` now maps the Meniga `Toast message` node `178:15386` into `src/app/components/ui/ToastMessage.tsx` and registers it as `ui.toast-message`, exposing Action required, Aware, and Google Pay toast variants without changing existing runtime toast consumers.
- Design System Inventory `Components -> Cards and content blocks` now treats the former `Debit Card` specimen as the canonical `Card` component, backed by `src/app/components/cards/Card.tsx`, with a selector for MC Debit Gold, MC Credit Premium Gold, MC Credit Partner Standard, MC Debit Standard, MC Virtual Standard Electric Violet, and MC Virtual Standard Vibrant Orange variants sourced from Meniga Harmonization Icons nodes `3039:30713`, `3039:7485`, `3039:8064`, `4161:9198`, `3039:12315`, and `3039:12380`.
- Design System Inventory `Components -> Products and country variants` now maps the Meniga `Product card - evolution` component set resolved at node `8724:1885` from the requested share node `9201:7443`, exposing `Product card / list / total row - evolution` with PI app and SME app Default, Accordion, and Open variants while keeping existing product-card runtime callers on legacy behavior unless they opt into `variant="evolution"`.
- Design System Inventory `Components -> Cards and content blocks` now exposes the Meniga `Shopsmart` card variants from nodes `9185:16470` and `9185:16260`, backed by `src/app/components/shopsmart/ShopsmartOfferCard.tsx` and registered as `products.shopsmart-offer-card`.
- Design System Inventory now also maps Meniga wallet/payment and filtering primitives: `Wallet buttons` from nodes `7464:1768`, `7464:1858`, and `7464:1912` for Google Wallet, Apple Wallet, and Click to Pay; `Bar` from node `1058:22155`; `Date filter` from node `290:14520`; and `Pill sorting` from node `290:14610`, all registered as reusable `ui.*` components without altering active runtime consumers.
- The Design System color inventory now also catalogs the active PFM semantic category colors used by Spending and Account Detail, not just the original screenshot-extracted core palette; PFM dark-mode partners now preserve each light category hue/chroma with an OKLCH perceptual lightness lift, so Spending category rows and icons remain colorful and readable in dark mode.
- in-app hide/show amount privacy for account/card/product balances, with transactions intentionally left visible.
- Account Detail transaction search for the current account/product mock transaction profile, including clear reset, activation scroll behavior, and a CZ Daily Banking Figma-inspired advanced filter bottom sheet from the search filter icon. The sheet covers keyword/account/variable-code fields, date presets, amount fields with country currency, status/type/category selector rows, remove-filters state, and a disabled/enabled Apply action; text, amount, status, type, and category filters are applied to mock transactions, while selector sub-sheets and date-window filtering remain future work. Current accounts now use country-specific merchant/counterparty transaction profiles across all 23 PFM categories, while saving accounts and term deposits intentionally show only own-account transfer in/out activity.
- Account Details info fields are now rendered through a reusable `AccountDetailsInfoField` component with a default title/subtitle variant and a trailing-icon variant for rows such as account number/copy.
- Messages mock Inbox/Outbox screen reconstructed from template 52, accessible from top-level header Messages icons across all PI countries, using the shared `PageHeader` and extended mock rows so Inbox/Outbox scrolling can be tested inside the phone frame.
- Contacts now uses the shared `PageHeader` collapsed-title behavior and shared `SectionHeadingDivider` section separators, aligning it with the other scrollable detail/list pages.
- Transaction Detail and Domestic payment mock flow, entered either from Account Detail transaction row -> Redo payment or Payments -> New payment -> Domestic payment; Transaction Detail now displays its top category pill from the normalized PFM category taxonomy and shared PFM icon component.
- Analytics / My Spendings mock screen for the Spending bottom-nav tab, now deriving inflow/outflow and Money Out / Money In PFM category summaries from the same enriched account transaction profiles used by Account Detail, with static demo FX conversion into each country's local currency, internal own-account transfers excluded from PFM spend/income totals, and a swipeable top hero that slides between monthly and yearly periods over the last two years.

## What This Project Is Not Yet

It is not yet:

- a full SME implementation with real SME screens;
- a fully implemented next-design-system mobile experience;
- a full multi-country Kids implementation; only SK/HU Kids concepts are active, while RO/RS Kids runtime apps have been deleted and other Kids markets remain planned placeholders;
- a real banking authentication/authorization layer; the Vercel access gate is only a stakeholder-demo distribution password and does not replace app login, user identity, tenant isolation, audit, or banking session authority;
- a backend-integrated Kids product, with real activation, legal consent, wallet/card operations, ledger posting, notifications, persistence, or audit trails;
- a backend-integrated banking application;
- a backend-integrated investments, trading, suitability, or fund-advisory application;
- a real payment-execution, ledger, or transaction-posting application;
- a persistent audit/release management system;
- a real release publication workflow with persisted baseline promotion history; Phase 1 models the ledger and readiness checks in source only;
- a real permission/entitlement backend; Phase 1 models mock rights and limits for stakeholder demos only;
- an automatic AI screen generator/compiler; the AI catalog, code-derived Figma JSON export, and local bidirectional `UniCredit Build UI Bridge` plugin support handoff/reuse workflows, but the app does not yet generate or mount new runtime screens from prompts by itself.

## Current Architectural Direction

The project should evolve into:

```text
demo platform + product catalog + screen/flow registry + release/baseline engine
```

The architecture must support:

- `PI`, `SME`, and `Mobile PI Kids`;
- all target CEE countries;
- current and future design systems;
- baseline/UAT states;
- release evolution;
- structured AI catalog export;
- AI-assisted flow construction from known screens and components;
- progressive conversion of screenshot templates into reusable code-backed screen primitives;
- typed template contracts that make every reusable template a standalone app page/state pattern with explicit component, screen, flow, data-source, and do-not-invent guidance;
- centralized icon and color registries that let AI reuse known DS assets instead of inventing local variants; active product screens should not import `lucide-react` directly outside the `AppIcon` boundary;
- eventual integration into a larger project platform.
