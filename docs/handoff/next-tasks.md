# Next Tasks

Status legend: `todo` / `in_progress` / `done` / `blocked`

## Active

| Status | Task | Evidence / Notes |
| --- | --- | --- |
| in_progress | Close out and publish the complete 2026-07-28 Mobile PI workspace | User explicitly requested one complete local checkpoint and a Vercel Production publication after the final Flow Library review-surface contrast correction. Full test, typecheck, lint, audit, and production build are green; commit/deployment evidence will be recorded in `current-session.md`. |
| done | Close the remaining ZCode workspace cleanly on local main | RS Teens was fully retired while Serbia PI remains; HU Kids gained scheduled goal funding and goal management; Access Gate/templates use shared tokens; optional V8 coverage tooling is configured. Fresh staged `npm run verify` passed typecheck, lint, 65 files / 651 tests, six audits, and build. `Creator Mobile/` remains excluded. |
| done | Create a clean local checkpoint for the HU Kids goal-funding developments | Add Money is routed from Goal Detail into a balance-aware account picker and calculator/keypad screen, applies the result to the selected goal, and returns to detail. Fresh `npm run verify` passed typecheck, lint, 66 files / 662 tests, six audits, and build. `Creator Mobile/` remains root-ignored and outside the checkpoint. |
| done | Unify the accumulated 2026-07-24 workspace while excluding Creator Mobile | `Creator Mobile/` is root-ignored and remains physically intact/untracked for extraction into its future host project. The main snapshot combines CZ NLU/free-text goal amounts, reply feedback, typed Flow Library architecture, and RO/RS Teens concepts. Integration fixes restored `.tsx` parsing and safe preview decimals. Fresh evidence: typecheck and lint green, 66 files / 662 tests, all six audits, and production build. |
| done | Publish the remaining Design System and Basket carousel workspace cleanly | Complete worktree staged by explicit user request; fresh `npm run verify` passed typecheck, lint, 61 files / 606 tests, six audits, and build. Source `26fd27d` is on `origin/main`; Production `dpl_3XfcSSifncbkhiytwGAEFntZXzWV` is READY; canonical HTTP smoke returned 200 and the recent error scan was empty. Final evidence checkpoint is pushed and redeployed before closeout. |
| done | Unify, publish, and deploy the complete 2026-07-22 workspace | Unified source commit `eea7c9c` passed the full 603-test gate, was pushed to `origin/main`, and deployed READY to Vercel Production as `dpl_2vaoZje5JFPtcKGuCJQJ59Q5gBcs`; canonical alias smoke returned HTTP 200 with no recent deployment errors. |
| done | Extend the Primary Button implementation-package pattern to every Design System component | 2026-07-22: `ComponentImplementationPackage.tsx` and `ComponentDetailScreen.tsx` are now fully generic (states/live-preview resolved via `componentId`, not hardcoded to Primary Button). New `componentLivePreviews.tsx` and `componentStatePreviews.tsx` registries back the View tab and the package States section for all 51 components, reusing the same real specimen components already in the catalog. 10 new `COMPONENT_REGISTRY`/`ComponentId` entries and 2 id-mismatch fixes (`dialogs.logout-confirmation`, `accounts.transaction-search`) were needed to reconcile registry/sample/href ids. Final unified gate green (typecheck, lint, 603/603 tests including the 206-test fail-closed coverage suite, all 6 audits, build); browser-verified in the Design System with 3 simple + 3 complex + 1 variant-rich + 1 image-bearing + 1 interactive component, Light/Dark, React/Swift/Kotlin, and non-regressed Primary Button. |
| done | Add Basket Funds to the CZ securities catalogue across current and future releases | CZ `Buy securities` now includes the 20-card carousel, All products / Regular Plan filtering, and the grouped Basket Funds page with independent expansion. Focused 4/4, related Investments 60/60, typecheck, build, and live CZ current/future plus RO isolation checks passed. |
| done | Implement Investments Sell Order across all Mobile PI countries | Product Detail Sell now runs Units/Amount or CTS-driven Units-only Order Data, inline quantity/amount validation, Review, terms, Face ID Sign, and Success for all eight countries. Focused 33/33, audit, build, and live port-4001 evidence are recorded in `current-session.md`; execution remains mock-only. |
| done | Pilot a portable implementation package on Primary Button | `ui.primary-button` now exposes React, SwiftUI, and Jetpack Compose references together with visual specs, states, motion, accessibility, and asset requirements. Both View and package state previews reuse the live component, the package is supplier-neutral, and unrelated detail pages remain unchanged. Full evidence is recorded in `current-session.md`. |
| done | Roll out the portable implementation package to every Design System component | All 51 components reachable from Design System -> Components now have a full package (summary, visual specs, states with real live-component previews, motion, accessibility, assets), React/Swift/Kotlin samples, and a wired View-tab live preview. A fail-closed coverage test (`tests/screens/component-implementation-package-coverage.test.tsx`) parses every `<Specimen>` in the catalog source files and fails if a future specimen ships without a `detailsHref` or a complete package. Full evidence in `current-session.md`. |
| done | Route investment-goal CTA directly to the matching fund collection | The portable action now carries `conservative`, `balanced`, or `equity` from the selected risk comfort. Live port-4001 evidence confirmed the Balanced path rendered the collection directly with no generic storefront intermediary. |
| done | Gate shared runtime Sign screens with Face ID before Success | `StandardSignScreen` now reuses the existing login/card-reveal Face ID animation and calls each flow's completion callback only after authentication finishes. Domestic payments, investment BUY orders, and the CZ credit-limit flow inherit the same behavior without per-flow copies. Focused RED/GREEN and live port-4001 credit-limit evidence are recorded in `current-session.md`. |
| done | Complete the investment-goal and credit-limit chatbot stories | Investment goal now collects five explicit inputs, renders a deterministic illustrative recap, and hands directly to the risk-matched Funds collection. Credit limit now converges through optional impact guidance into Review -> terms -> PIN -> Success, applies a session-only card/available-credit override, and removes the consumed offer. Focused RED/GREEN and live port-4001 evidence are recorded in `current-session.md`. |
| done | Compact the sticky Investments collection-detail heroes | The full collection safe-area/controls/hero remains fixed, but the hero is now 132px high with a one-line title plus maximum three-line subtitle; exact supplied imagery crops through `object-cover`. Focused RED/GREEN and live Balanced-detail geometry/visual evidence are recorded in `current-session.md`. |
| done | Complete remaining component code samples for Design System detail pages | The 6 components with no prior sample (shell.home-header, shell.more-header, ui.button-registry, ui.generic-controls, prelogin.other-panel, prelogin.other-panel-basic) now have real React/Swift/Kotlin samples; every other Components-tab id already had one and was left untouched. `COMPONENT_REGISTRY` grew from 86 to 96 entries (`npm run audit:templates` reports `components=96`). |
| done | Wire remaining unwired Specimen cards to detail pages | Every `<Specimen>` in DesignSystemPage.tsx / specimens/cardSpecimens.tsx / specimens/fieldSpecimens.tsx now has a `detailsHref` (Dropdown, Generic UI controls, Button registry, Home/More headers, Wallet buttons, Bar, Divider, Amount/Code field, Date filter, Pill sorting, Toggle button, Navigation row, Profile avatar, Card, Shopsmart, AccountSearchBar, Toast message, Product card/list/total row, PanelWithTranslations/Basic, and more), each pointing at a component with a complete package. |
| done | Finish the selected-investment chatbot chart and connected period controls | The first product-explanation card uses the canonical Investments chart and shared chips for `1 M` / `3 M` / `1 Y` / `3 Y` / `ALL`, defaults to `3 Y`, switches real typed series, and propagates the selected security's price/currency. The application injects the renderer so the portable chat package has no reverse dependency. Focused 33/33, full 345/345, and interactive browser evidence are recorded in `current-session.md`. |
| done | Make selected-investment performance visual-first and connect Buy/Buy more | The canonical card precedes non-duplicative interpretation copy. Buy/Buy more now collects quantity, canonical cash account, and execution timing conversationally, validates quantity/balance, then transfers the exact typed draft to existing `Review Data`; terms, Sign, and Success remain in the coordinator. Held, catalogue-only, invalid, unknown, replay, and completed-review cases have focused regression and live browser evidence in `current-session.md`. |
| done | Ground CZ Future chat in the selected Investments security detail | Exact product context drives an intentionally small two-topic entry shelf with genuinely different responses: Explain defines the balanced-fund mechanics, risks, monthly dealing, and document checks without repeating position metrics; Performance handles value/units/return. The Product Detail `32x32` logo card appears once on the first product answer, while later answers keep only their relevant text and actions. Performance, risk, and documents chips have distinct integrated click tests and live browser evidence; named-product recovery also survives cleared screen context. Evidence is recorded in `current-session.md`. |
| done | Add connected Payments Templates and Exchange Rates shortcuts | Templates/beneficiaries search and prefill the existing Domestic payment flow; FX amount/source selection recalculates from one authority and uses local SVG flags plus compact DS controls. Focused 9/9, browser interaction/no-console-error smoke, and final gate evidence are recorded in `current-session.md`. |
| done | Add connected PFM Spending category drill-downs, reversible bubble filtering, and period navigation | All populated Money Out/Money In rows open the production-reference detail composition. Subcategory bubbles use a collision-free layout, remain visible in a neutral inactive state when filtered, and toggle back on to restore totals/transactions. The same timeline now supports touch and desktop drag/snap through monthly, current-year, and previous-year panels; session recategorization feeds the shared aggregation authority. Final focused/browser/full-gate evidence is recorded in `current-session.md`. |
| todo | Upgrade GitHub Actions off the deprecated Node 20 action runtime | The successful 2026-07-19 `verify` run `29687079235` warns that `actions/checkout@v4` and `actions/setup-node@v4` target Node 20 and are being forced onto Node 24. Upgrade in a dedicated CI-maintenance change; this does not block the current green publication. |
| done | Unify and publish the 2026-07-18/19 health and refactor work | All 14 local-only commits through `bb8257d` were pushed to `origin/main`; the only secondary remote branch was fully merged and deleted; one `main` worktree, no stash, no local side branch, and a clean worktree were confirmed. Fresh `npm run verify` passed with 234/234 tests, all six audits, and the production build. The final closeout commit is followed by an explicit Vercel Production deployment and authenticated smoke. |
| done | Split the six remaining monolith files | 2026-07-18/19, one commit each, every one verified as a pure move (declarations byte-identical in their new homes, none missing, none added) with `npm run verify` green after each: `DesignSystemPage.tsx` 4,371 -> 462; `TemplateCodePreviews.tsx` 2,836 -> 138; `KidsMarketHomeApp.tsx` 2,725 -> 977; `AppIcon.tsx` 2,135 -> 249; `czChatOrchestration.ts` 1,816 -> 1,335; `phoneScreenshot.ts` 1,744 -> 309. Two audits were taught the new file layout without changing what they assert: `audit-template-contract.mjs` reads the preview-id union from `templateData.ts`, and `audit-figma-bridge.mjs` reads the whole exporter folder. |
| todo | Break up `buildCzChatSmartReplyResolver` | The last real monolith is a single 1,285-line function with 87 inner bindings inside `czChatOrchestration.ts`. Splitting it means restructuring a function body rather than moving declarations, so the byte-identical guarantee used everywhere else does not apply. Needs its own pass, leaning on `tests/chat/*` for cover. |
| todo | Finish Kids split Phase 4 | `KidsMarketHomeApp.tsx` is now the dispatcher plus `HuCeeLightRestyleApp`, the 744-line HU state container. Extracting that container leaves the small dispatcher the plan asked for. |
| done | Delete the dead Figma import dump in `src/imports` | 2026-07-18: 72 of 77 files removed (14,942 lines). Proven dead four ways: reachability from all 49 entry points, brute-force textual search over 537 repo files, bundle inspection, and a real `git rm` deletion run through the whole gate. The five surviving `svg-*` modules are used by `FloatingCoAppingButton`, `StatusBar`, `UniCreditLogo`, `Card`/`DemoTopBar`, and `useProducts`. The apparent bundle hits were live components holding their own copies of the same generated Figma markup (`PanelMenuSheet`, `StatusBar`, `AppIcon`). |
| done | Split the oversized session log | 2026-07-18: `current-session.md` went from 690 KB / 6,293 lines to 22 KB / 177 lines; 209 older sessions moved verbatim into `docs/handoff/archive/sessions-2026-06.md` and `sessions-2026-07.md`. Zero sections and zero content lines lost, verified by comparison. The file had grown past the point where any agent could open it, which broke the `agents.md` rule to read it first. |
| done | Enforce the quality gate in CI | 2026-07-18: `.github/workflows/verify.yml` runs `npm run verify` on push to `main`, on pull requests, and on demand. Nothing enforced it before, which is how the working tree reached five type errors and two failing tests unnoticed. The gate is **green**: 0 type errors, ESLint clean, 234/234 tests, six audits, production build. |
| done | Close the two remaining type errors | `selectedPendingActionId` in `KidsMarketHomeApp.tsx` now binds only its setter, since nothing reads the value yet. `TutorialDetailOverlay` no longer destructures `onBack`; the prop stays on the interface and at the call site because the caller genuinely wires "return to the tutorial list", but the header renders only a close control. |
| todo | Render the Tutorials detail back control | `TutorialDetailOverlay` receives a working `onBack` and reserves a 32px slot on the left of its header, but no back button is drawn there — only Close is. Returning to the tutorial list without closing the sheet is therefore unreachable. Needs a UX decision before wiring. |
| done | Repair the two failing HU Kids Learn tests | 2026-07-18: both were stale rather than broken product code. Earning now shows two `SHOW MORE` links (Tasks and Education), so the query is scoped through `[data-hu-learn-education-card]`. Messages is asserted on the Earning header before navigating, because the Learn page swaps that header for the menu frame. The lesson player was redesigned — the button is `NEXT` and the "Slide n of m" caption is gone — so the segment rail is now the progress contract. |
| todo | Give the two Earning `SHOW MORE` links distinct accessible names | Tasks and Education both expose a button whose accessible name is exactly `SHOW MORE`, on the same surface. Tests can scope around it, but a screen-reader user hears the same label twice with nothing to tell them apart. |
| done | Add the missing `ES2023.Intl` TypeScript lib | 2026-07-18: `useGrouping: "always"` in the HU Kids money helpers is valid ES2023 and correct at runtime, but `tsconfig.json` only loaded `ES2020`, so three of the five type errors were a configuration gap rather than a code fault. Adding `ES2023.Intl` to `lib` cleared exactly those three and introduced nothing new; lint, tests, audits, and build all still pass. |
| done | Complete nine-direction product health hardening | Isolated reversible branch, 47 implementation/audit commits after `453e7e2` plus this closeout; 231 tests, typecheck, lint, all audits, build, diff check, and five-route browser smoke passed. See `product-health-revert-map.md`. |
| todo | Review asset audit candidates visually before any PNG optimization | `npm run audit:assets` locks 159 tracked assets to the cross-platform Git-canonical aggregate `2452305f...`; 36 conservative candidates and four exact duplicate groups are reports, not deletion authorization. Update the baseline only with explicit approval and before/after visual evidence. |
| done | Align global Mobile PI Card Details with Face ID and the Meniga Figma screen | Debit and credit cards in all eight PI countries now gate both Card Details entry points behind shared Face ID, then render the four Figma fields using the platform header. Card number copies through the shared clipboard/toast behaviour; `npm run audit:card-details`, build, template/platform audits, and diff check passed on 2026-07-15. |
| done | Reconcile the Mobile PI Investments mocked portfolio across all tabs and countries | Canonical config now creates 10 active and 2 inactive owned positions. `npm run audit:investments` checks RO/CZ/SK/HU/RS/BA/BA_BL/SI totals, distribution values/percentages, price × quantity and active/inactive status. |
| done | Publish the 2026-07-15 Investments distribution-controls update | Product commit `8fb1831` was pushed to `origin/main`; Vercel production deployment `dpl_GciexcyciAbjBhUw2dEMwHFcYh9w` reached `READY` with the canonical `https://mobile-banking-cee.vercel.app` alias and no alias error. |
| done | Close and commit the complete 2026-07-15 workspace | All tracked/untracked Card Options, Card Details, HU Kids, frost asset/license, and documentation changes are included in the requested local `main` checkpoint; build, template audit, platform audit, and diff check passed. No push or deployment was requested. |
| done | Replace HU Kids procedural freeze polygons with photographic frost | `KidsMarketHomeApp` now uses a local CC0 window-frost asset at 40% final opacity; the full-surface animation resolves from blurred depth toward the viewer, the available-to-spend amount is hidden only while frozen, Block/Unblock browser smoke passed, and `npm run build` passed on 2026-07-14. |
| todo | Optimize HU Kids frost asset delivery without changing the approved visual | The photographic frost source builds to about 2.09 MB. Preserve its current 40% visual treatment while evaluating lossless/visually equivalent compression in a dedicated asset-performance pass. |
| done | Align account filters and Connected cards heading | Account transaction filters use the shared safe-area reserve and Account Details Info uses `SectionHeadingDivider`, matching the rest of the app. |
| done | Commit and publish complete workspace | The latest commit was pushed to `origin/main`; the Vercel production deployment is READY at `https://mobile-banking-cee.vercel.app`. |
| done | Align global debit/credit Card Options with RO Enablers | Card Options now uses the exact shared/credit/debit row sets, supplied SVGs, standard section divider, no card identity block, and no separators between rows for all Mobile PI countries; Card Detail also reserves 24px vertically around `SHOW CARD DETAILS`. Downstream option workflows remain a follow-up when their product behavior is specified. |
| done | Align demo header Play icon with supplied Design System SVG | `AppIcon` owns the exact 24x24 circular glyph and `DemoTopBar` renders it at native size in the existing button slot; browser smoke confirmed the path, `#262626` color, and visible Design System inventory entries. |
| done | Unify Investments negative-state red | History transaction/order negative values now use exact `#CF3524`, matching negative performance and sell states. |
| done | Verify Investments product-detail sticky header system-bar clearance | In-app browser smoke on 2026-07-13 confirmed the initial hero title remains visible, the compact title stays below the status bar at deep scroll, and no runtime errors were reported. |
| done | Restore Investments portfolio header safe-area behavior | Shared `PageHeader` keeps the top reserve inside the sticky header at `top: 0`; initial large title and compact scroll title are both verified. |
| done | Verify Investments catalogue visual consistency | In-app browser smoke on 2026-07-13 confirmed the security-list logo wrapper/ SVG sizing, #3D7D43 positive text, and the large-left to centered-small title transition; no runtime errors. |
| todo | Verify Investments distribution/calendar visual regression after latest fix | Browser smoke on 2026-07-13 confirmed connector rails stop before labels, calendar day 15 is visible/selected, and `Select interval` overlays `Apply filters`; add committed regression coverage for these states. |
| todo | Verify Investments product-detail data across owned and catalogue-only variants | Confirm Help is absent, owned hero amount matches `MY SECURITY` local value, catalogue-only hero retains instrument currency, and last-update text is generated for all countries. |
| todo | Add automated Investments visual regression coverage | Current browser pass covers dense monotone chart data, native 32px trade glyphs/Buy teal, 24px detail action spacing, and single-visible-title collapse state; add a committed browser test for scroll-triggered sticky title and all period curves. |
| todo | Add automated Investments catalogue/detail/history/chart browser coverage | Cover Performance -> curve/point tooltip and no auxiliary grid, distribution-tab donut/list rendering without overlapping labels, Invest -> search/filter -> not-owned detail, portfolio card -> owned detail, Back restoration, amount masking, all-country currency resolution, History custom calendar/date/type/currency/status filters, applied-chip removal, Transactions/Orders tab behavior, and CZ Future chatbot overlay preservation. Build/audits passed on 2026-07-10, but authenticated browser automation tooling was unavailable in that runtime. |
| done | Commit and publish Investments logo/distribution chart polish | Closeout requested on 2026-07-08; `npm run build`, `npm run audit:templates` (`templates=47 codePreviews=47 components=79 screens=27 flows=14`), `npm run audit:platform`, and `git diff --check` passed before commit/deploy. |
| done | Commit current Investments and Design System unification edits | Closeout requested on 2026-07-08; `npm run build`, `npm run audit:templates` (`templates=47 codePreviews=47 components=79 screens=27 flows=14`), `npm run audit:platform`, and `git diff --check` passed before commit. |
| done | Close out and publish current CZ Chatbot/Product detail workspace package | Full workspace closeout requested on 2026-07-07; `npm run audit:templates`, `npm run audit:platform`, `npm run build`, and `git diff --check` passed before commit/deploy. |
| done | Install AI Contributor Operating System docs | `agents.md`, `docs/handoff/*` |
| done | Add project architecture model | `docs/architecture/PROJECT_MODEL.md` |
| done | Add typed taxonomy and registries | `src/app/registry/projectModel.ts`, `screenRegistry.ts`, `flowRegistry.ts`, `releaseRegistry.ts` |
| done | Add Phase 1 Reference Platform architecture | `docs/architecture/REFERENCE_PLATFORM_PHASE1.md`, `baselineRegistry.ts`, `featureManifestRegistry.ts`, release diff/readiness support, and project-pack registry |
| done | Add banking scenario and entitlements infrastructure | `bankingScenarioRegistry.ts`, `effectiveAppContext.ts`, and `bankingRepositories.ts` model mock holdings, rights, limits, visible products, enabled actions, disabled-action reasons, and contract-ready repositories |
| done | Cover future SME and Kids application variants by country | `projectPackRegistry.ts` generates 24 product/country packs across `PI`, `SME`, and `KIDS_PI` for all 8 country/application variants; SME remains prepared/planned, SK/HU Kids are active mock-driven concepts, and RO/RS/CZ/BA/BA_BL/SI Kids contexts are planned placeholders |
| done | Add Card design-system component | `src/app/components/cards/Card.tsx`, Design System `#cards` specimen, and `cards.card` registry entry now own the Meniga Mastercard card family; the old card SVG asset was removed after Card became the canonical artwork source |
| done | Implement CEE homepage updates and transaction detail polish | Renamed Savings/Loans/Investment accordion titles, formatted dynamic lookalike IBANs, mapped PFM ATM to Wallet and FX/Internal to Transfers, extended gray header to transaction details screen, and updated Change Category icon to custom SVG |
| done | Enable "Between my accounts" / "Currency exchange" card by default | Removed requiredFeature gating from payments.exchange.create so the card is clickable in all countries; build and audits passed |
| done | Fix Contacts and Settings screen layout row padding | Adjusted page-level and NavigationRow paddings to occupy full viewport width as in Figma specs; build and audits passed |
| done | Run verification | Latest closeout verification on 2026-06-30: `npm run build` passed with the known chunk-size warning, `npm run audit:templates` passed, `npm run audit:platform` passed, `git diff --check` passed with only normal Windows LF/CRLF warnings, `node --check scripts/export-platform-icon-catalog.mjs` passed, and the platform icon catalog sanity check found `119` SVG code blocks. `typecheck`, `lint`, and `test` remain blocked by missing local scripts/tooling |
| done | Add Account Detail advanced transaction filter sheet | Added `AccountTransactionFiltersSheet` from the CZ Daily Banking Figma reference and wired the shared Account Detail `AccountSearchBar` filter button to open/apply/remove filters across Mobile PI countries. Build and in-app browser smoke on `5002` passed on 2026-07-01 |
| done | Define ShopSmart cards and rebuild Products ShopSmart tab | Added `ShopsmartOfferCard`, local optimized ShopSmart Figma images, Design System Inventory `Shopsmart` specimen, `products.shopsmart-offer-card` registry entry, and RO Enablers-style Products / ShopSmart summary, search, and offer-card list. Build, template audit, platform audit, diff check, and in-app browser DOM/visual smoke on `4001` passed on 2026-07-01 |
| done | Add RO Round Up future-flow preview library page | Added `FlowLibraryScreen`, `flowPreviewRegistry`, top-bar `Flows` navigation, deep-link support for `screen=flow-library&flow=ro-round-up`, and `platform.flow-library` registry coverage. Build, template audit, platform audit, diff check, and in-app browser visual smoke on `5002` passed on 2026-07-01 |
| done | Add RO Card PIN future-flow preview library page | Added `ro-card-pin` to the Flow Library with View credit PIN, View debit PIN, Change credit PIN, and Change debit PIN scenarios sourced from RO Enablers node `2247:16744`. Build, template audit, platform audit, diff check, and in-app browser visual smoke on `5002` passed on 2026-07-01 |
| done | Clean HU Kids Home, More, and Send money surface | Removed HU Kids More `Product applications and cancellations`, removed the Send money result card, renamed `Grandma` to `More contacts`, and moved Home `Your cards` above recent transactions. Build, template audit, and platform audit passed on 2026-07-02 |
| done | Add Baseline / Future selector model | Stakeholder header now exposes `Baseline` plus `Future`, with a second dropdown for compatible isolated future-feature previews. The first preview is `CZ - Chatbot` for Mobile PI Czech Republic; legacy R1/R2/R3/R4 bundles remain registry-compatible but are no longer visible in the header. |
| done | Add stakeholder header More menu and large demo preview | Header actions now show `Refresh`, Play / `Open large demo`, `Share`, and `More actions`; `More` contains Settings, Screenshots, and the current Light/Dark mode, while Play opens a large focus preview overlay for the existing demo frame. The normal preview body now also fills the available desktop height so the phone scales larger without a bottom blank band. Build and in-app browser smoke passed on 2026-07-06. |
| done | Consolidate stakeholder app and country selectors | Row one now shows a combined selector such as `PI - Czech Republic` or `Kids - Hungary`, with `App` and `Country` sections in the same dropdown. Row two now starts with `Baseline App` / `Future App`, followed by the future-feature selector when applicable. Build and in-app browser smoke passed on 2026-07-06. |
| done | Polish CZ Chatbot edge launcher overlay | Right-edge launcher is now rendered through the phone-frame overlay, uses a `32px` black SVG tab at `top: 604px`, avoids scroll clipping/seam artifacts, keeps underlying screen clicks working outside the tab, and opens/closes with the portal animation. Build, diff-check, and browser hit-test smoke passed on 2026-07-06. |
| done | Polish CZ Chatbot Home product shelf shortcuts | Home topic `What products can I open` is position 2, answers from the Products `OUR PRODUCTS` shelf config, and every rich product card redirects to Products / Banking / `OUR PRODUCTS` with the matching bottom sheet open. Build, diff-check, all-card browser sweep, production deploy, and redeploy passed on 2026-07-07. |
| todo | Expand CZ Chatbot `For you` into a real CRM campaign surface | Current implementation proves the credit-card limit-review scenario with a local opportunity model, segment-tab attention cue, proactive card-detail nudge, explicit `10 000 CZK` -> `15 000 CZK` plafon offer, card-identification row, contextual ranking, and guarded chat handoff. Next pass should define campaign eligibility API shape, suppression rules, frequency caps, multi-product ranking, analytics events, and real CRM payload mapping. |
| todo | Wire CZ Co-Apping conversation actions | The conversation detail `More` menu now exposes staged UI items for `Share`, `Rename conversation`, and `Delete conversation`; persistence/state mutation and confirmation behavior still need product decisions. |
| todo | Add automated coverage for CZ Chatbot rich responses | Manual browser smoke now covers Investments goal suggestions through goal type -> horizon -> amount -> monthly -> preview -> projection, including direct follow-up chip clicks and click-after-drag behavior, plus Home product-shelf shortcuts and For you carousel click-to-conversation plus drag-no-click behavior. Add a committed regression for these rich-response, carousel, and chip/drag paths. |
| todo | Add automated regression coverage for QR share access | QR mobile links now use short-lived share tokens that exchange into the normal access cookie, but coverage is still manual/API-simulation only. Add a committed test for token issuance, token exchange, URL cleanup, expired/invalid token rejection, and normal password fallback |
| todo | Define the full future-feature lifecycle and visual management rules | Current decision: a Future preview equals source baseline snapshot plus one feature, not a composed release train. Add a documented owner workflow for rebasing, conflict review, promotion into Baseline, retiring a Future item, and visual upgrade expectations when the source baseline drifts. |
| todo | Add licensed UniCredit webfont assets for exact mobile typography | Current font CSS resolves UniCredit only from locally installed fonts; no `.woff2`/`.ttf` assets are bundled. Add approved webfont files plus preload/reference rules when licensing and source assets are available |
| todo | Add automated browser coverage for the two-line stakeholder header and Flow Library deep links | Manual browser smoke covered product selector, centered scenario switch, header action ordering, and Flow Library routing. Add a regression that verifies PI/SME/Kids switching, `Demo`/`Flows`/`Design system` navigation, no header scrollbar, and `screen=flow-library&flow=...` restoration |
| todo | Add Account Detail filter selector sub-sheets and date-window logic | The Account Detail filter sheet renders the Figma status/type/category selector fields and date presets, but dedicated option sheets and date-window transaction filtering still need a follow-up pass |
| todo | Replace shared ShopSmart mock content with country-specific specs | Current ShopSmart offer cards and summary values are shared from the RO Enablers Figma reference for the existing ShopSmart-tab countries. Final per-country merchant lists, translations, activation states, click-through behavior, and availability rules still need product input |
| todo | Wire RO Round Up into a real release when product scope is approved | The Flow Library preview is intentionally separate from the current baseline. Once release timing, eligibility, legal copy, account-opening rules, and destination behavior are confirmed, wire Products `Saving and investing > Round Up` to the executable flow and add final country rules |
| todo | Wire RO Card PIN into Cards when product scope is approved | The Flow Library preview is intentionally separate from the current baseline. Once Card options entry points, biometric rules, eligibility copy, and per-card PIN behavior are confirmed, wire Cards/Card options `View PIN` and `Set your PIN` into executable runtime flows |
| done | Kids split Phase 1 retired by deleting RS Kids app | The planned `rs/` extraction became obsolete when the user approved deleting Serbia Kids. `RsKidsSafeSpendApp`, RS data/types, RS registry entries, and RS style hooks were removed instead of preserved. |
| done | Kids split Phase 2 — extract `sk/` from KidsMarketHomeApp.tsx | 2026-07-18: the whole non-HU tree moved into `sk/data.ts` (SK_TASKS/SK_LESSONS/SK_MORE_ITEMS), `sk/SkBulbankScreens.tsx` (13 `Sk*` surfaces), and `sk/ConceptShell.tsx` (header, hero dispatcher, action grid, pockets, coach, activity, bottom nav) — 817 lines, zero inbound dependencies. `tests/screens/sk-kids-tree.test.tsx` was added because the only committed test touching SK fails earlier on an unrelated HU assertion. |
| in_progress | Kids split Phase 3 — extract `hu/` from KidsMarketHomeApp.tsx (sub-phased) | 2026-07-18: done — `hu/types.ts` (24 types), `hu/data.ts` (HU_* seed constants + HU_KIDS_TRANSACTIONS, 461 lines), `hu/learnTopics.ts` (the Learn curriculum, 2,478 lines, split out so `data.ts` stays readable), `hu/money.ts` (formatHu*/grouping), `hu/learnArtwork.ts` (artwork registry + the 20 Learn PNG imports, now `@/assets/...`), `hu/merchantLogos.tsx`, `hu/goals.tsx`, `hu/transactions.tsx`, `hu/cardDetails.tsx`, `hu/chrome.tsx`, `hu/learnScreens.tsx`. Remaining in the dispatcher: `HuCeeLightRestyleApp` (state container), the theme-change/preview/carousel group (needs `HuHomeContent`), payments/more pages, request/send money, tasks, and the Home/Saving/Earning content blocks. |
| todo | Remove the unreachable `hu-smart-fintech` branches in `sk/ConceptShell.tsx` | Surfaced by the 2026-07-18 kids split. `KidsMarketHomeApp` returns `HuCeeLightRestyleApp` before `ConceptHero` renders, so inside `sk/ConceptShell.tsx` the whole `HuSmartHero` component plus the `concept.style === "hu-smart-fintech"` branches in `ConceptHero`, `PocketSection`, `PocketCard`, and `ActivityRow` can never execute. Left in place because deleting a concept surface is a product decision, not a refactor one. Confirm the HU smart-pocket concept is retired, then delete. |
| todo | Kids split Phase 4 — slim dispatcher | After Phase 3 completes: `KidsMarketHomeApp.tsx` (now 2,724 lines, down from 9,292) should end up a small dispatcher routing only to the `hu/` and `sk/` entry points. |
| todo | Verify HU theme contrast fix on Payments/More L1 pages visually | `getHuThemeStyle` + `HuKidsPiMenuFrame` were edited this session (subpage-bg 22% accent, pi-card-bg white, stronger shadows, fully opaque header) to fix "dreadful and broken" contrast. Build passes but user has not yet confirmed visual result. |
| in_progress | Optimize oversized referenced image assets and add an asset-size guard | 2026-07-18: the three shipped Figma exports were re-compressed losslessly (40.61 MB -> 34.28 MB, `dist` 64.3 MB -> 57.9 MB). Pixel data is provably unchanged — each file was decoded before and after and the SHA-256 of the raw samples matched — so only deflate settings, row filters, and an `eXIf` chunk differ. The real remaining problem is dimensions, not compression: `b756062d…` is `7397x5011` and `befcf832…` is `6144x4096`, both rendered as small cards. Downscaling would recover roughly 95% but changes pixels, so it needs explicit approval and before/after visual evidence. No asset-size guard exists yet. |
| todo | Decide the fate of the two now-unreferenced large images | After the dead `src/imports` deletion, `ff8ceb68….png` (48.6 MB, `4598x6890`) and `cb092756….png` (0.6 MB) have zero references from live code. They were deliberately kept, because the standing instruction is that every existing image stays. Deleting them would remove 49.2 MB and needs an explicit decision. |
| todo | Review the empty `react-vendor` manual chunk | `npm run build` passes but reports `Generated an empty chunk: "react-vendor"` after the manualChunks refactor. Confirm whether React is being merged elsewhere by Vite/Rollup and simplify the chunk map if needed. |

| done | Add HU Kids flagship theme "Nordlys" | Dark polar-night hero with 5-layer CSS motion (veil, two silk beams, prism glint, gold ember), mode-stable field literals, frosted hero controls, product-blue-deep accent; theme model extended backward-compatibly (motionLayers/motionMask/hero overrides); build, both audits, diff check, and browser smoke (select/preview/apply/scroll-fade/dark mode, no console errors) passed on 2026-06-11 |
| done | Extend HU Kids applied themes across all bottom-nav sections | `HuKidsPiMenuFrame` is theme-aware, and `Payments`, `Products`, and `More` now inherit the active HU theme via ambient motion, body wash, header/icon foreground, card-neutral variables, and `data-hu-kids-themed-section`; `Spending` keeps the Home/Saving themed layer. Build, both audits, targeted diff check, and in-app Browser smoke on `3005` confirmed Nordlys across `Spending` / `Payments` / `Products` / `More` in light and dark mode with no console errors |
| done | Wire HU Kids `Request money` quick action to a local request flow | `src/app/screens/kids/KidsMarketHomeApp.tsx` now opens a themed request-money screen from the Home/Saving quick action, creates a local pending action after submit, and shows the pending-action carousel on Home with corrected 24px gutters and multiple notification card examples. Build and both audits passed on 2026-06-11; visual browser click-through remains recommended because browser automation could not attach to the in-app tab in this run |
| done | Open HU Kids card details from `Your cards` artwork | `src/app/screens/kids/KidsMarketHomeApp.tsx` now opens a HU-local themed `card-details` view from each `Your cards` card image, using a PI Card Details-like structure while keeping HU Home-style recent transaction rows. Build, both audits, and targeted diff check passed on 2026-06-11 |
| done | Show merchant logo for HU Kids McDonalds transactions | `HuTransactionRow` in `src/app/screens/kids/KidsMarketHomeApp.tsx` now supports a Kids-only `merchantLogo` prop; the McDonalds rows on HU Home and HU card-details use a local merchant-logo renderer as an Ethoca placeholder while PI transaction rows remain unchanged. Build and both audits passed on 2026-06-11 |
| done | Polish HU Kids transactions, merchant logos, transaction detail, and card-details layout | HU Kids Home/Card Details now use shared `AccountTransaction`-compatible transaction data, Kids-local vector merchant marks for McDonalds/YouTube/Apple, click-through to the shared PI `TransactionDetailScreen`, and a PI Card Details-inspired card/search/action/transaction layout. Build, template audit, platform audit, and in-app browser smoke passed on 2026-06-12 |
| done | Add HU Kids Card Details Face ID reveal and freeze/unfreeze | HU Kids Card Details now removes the leftover Free To Spend carousel/show-details link, uses a compact theme-blended header, renders a Home-style recent-transactions container, reveals copyable card number/expiry/CVV through the existing Face ID animation plus card flip, and toggles a frozen card overlay through Block/Unblock. Build, both audits, targeted diff check, and in-app browser state inspection passed on 2026-06-12 |
| done | Fix HU Kids Home pending carousel, single-card `Your cards`, and Send money quick action | `HuRequestMoneyRail` now supports mouse/touch drag with click suppression; `Your cards` is a clean Figma-sized single `Mastercard Standard *4007` row; and the HU `Send money` quick action opens a local transfer flow that adds a Home notification after submit. Build passed and in-app browser smoke on `5173` confirmed carousel `scrollLeft` moved from `0` to `281`, no `Add new card`, Send money submit/return-home behavior, and no console errors |
| done | Polish HU Kids header controls, amount masks, and Card Details transaction structure | HU Kids header icon controls are smaller, hidden amounts now use PI-style `****` / `****,** HUF` masks instead of bullet/corrupt glyphs, and Card Details now uses large action buttons on gray background, search above the list, and Design System `AccountTransactionMonthDivider` day/net-total separators. Build and in-app browser smoke on `5173` passed with no console errors |
| done | Centralize HU Kids applied-theme inheritance for all pages | `HuCeeLightRestyleApp` now wraps all HU Kids views in one `HuThemeShell` with `data-hu-kids-theme-scope`, while non-Standard themes remap shared DS tokens so Home, Spending, Payments, Products, More, Change theme, Request money, Send money, Card Details, and shared PI Transaction Detail inherit the applied theme automatically. Build, template audit, platform audit, and in-app browser smoke on `5173` passed in light and dark token modes |
| done | Redesign HU Kids theme selection view layout and preview phone mockup | Adapted `HuThemeChangePage` and the preview mockup to respect `themeMode` (light/dark appearance), replaced the multiple-layered nested container with a clean realistic 5px black iPhone bezel with status bar and iOS home indicator, scaled preview content correctly to show it fully without bottom cropping, and refactored carousel buttons and labels to use theme-safe color tokens. Added Light, Dark, System appearance selector buttons below the theme carousel in `HuThemeChangePage` synced with global `themeMode` and local storage (with system media query listener). Fixed theme carousel buttons clipping by increasing scroll container padding to `px-[6px] py-[8px]`. Build and audits passed on 2026-06-13 |
| done | Add HU Kids Card Details copy toast | HU Kids Card Details copyable card number/expiry/CVV fields now show a bottom pill toast matching the supplied `Messages` JSON style, with `Account number successfully copied` for card-number copy and timed auto-dismiss. Build passed and in-app browser smoke on `5173` confirmed copy reveal, toast appearance, and auto-clear |
| done | Wire HU Kids Messages and Saving tab polish | Header Messages in HU Kids opens the shared PI Messages screen inside the HU theme shell; the second tab is now `Saving` with the supplied piggy icon, and the Saving page top actions are `Save money`, `Request money`, `Move money`, `Create goal`. Build, both audits, and browser smoke on `5173` passed with no console errors |
| done | Move HU Kids Goals and Learn onto HU-owned data | HU Kids Saving now uses HU-owned goal and Learn data in `src/data/huKidsBanking.ts`, with goal list/detail/create views, add-money progress updates, and Ask parent reuse of the HU request-money flow. HU Kids bottom nav tab 4 is `Learn` with `book-open`, and the page renders Learn modules with progress/Q&A/completion state. Build, both audits, and in-app Browser smoke on `5173` passed for Saving goals, Create goal, Goal detail/add progress, and Learn module rendering; late repeated browser clicks intermittently reset the demo tab to default PI/RO, so module-completion click needs a future visual recheck |
| done | Refactor HU Kids Learn cards into theme-safe topics | HU Kids Learn now uses topic cards, topic detail, and lesson detail pages with dedicated `--hu-learn-card-*` light/dark theme surfaces, standardized PNG slots (`98x92`, `140x122`, `166x146`, `100x88`, `250x230` logical px), and no card text line-clamp/ellipsis. Build, both audits, and browser smoke on `5173` passed on the active Blockcraft theme in light mode with no console errors |
| done | Apply supplied blockcraft savings image to one HU Kids Learn card | The user-supplied image was processed into `src/assets/kids/learn/saving-goals-blockcraft.png` and mapped only to the `Saving goals` topic cards as a real-card visual test. The Learn artwork slot now renders supplied PNGs transparently without the old artificial slot border and with a subtle theme-token halo. `npm run build` passed, and in-app browser smoke on `5173` confirmed the featured card image in light/dark mode with no console errors |
| done | Apply full HU Kids Learn artwork set | HU Kids Learn now imports optimized transparent `512x512` PNGs from `src/assets/kids/learn/hu-learn-*.png` for every visible topic card, topic detail hero, lesson row, and lesson detail hero. The rejected artificial halo/drop-shadow/border treatment was removed for image slots. `npm run build`, `npm run audit:templates`, `npm run audit:platform`, `git diff --check`, and in-app browser smoke on `5173` passed; browser confirmed six topic-card PNGs, topic hero PNG, three distinct lesson-row PNGs, lesson hero PNG, zero fallback slots, light/dark visual blending, and no console errors |
| done | Expand HU Kids Learn with 4 new financial topics and 12 lessons | Added 4 new topics (smart-budgeting, earning-money, digital-security, family-banking) and 12 lessons with 3 slides and a 2-question checkpoint quiz each. Mapped their IDs uniquely in `HU_LEARN_TOPIC_ARTWORK` and `HU_LEARN_LESSON_ARTWORK` to ensure zero duplicate illustrations on the main screen. Build, template audit, and platform audit passed on 2026-06-13 |
| done | Rerun HU Kids theme-bridge visual smoke when Browser attach is stable | 2026-06-13 closeout on `http://127.0.0.1:4001/` selected `Mobile PI Kids` + `Hungary`, applied Aurora, and verified `Payments`, `Learn`, and `More` with the new native subpage wash/card bridge and no console errors. Standalone `Mobile PI` + `Hungary` Payments was also verified so shared PI PaymentHero cards no longer become transparent. Dark-mode visual smoke remains covered by `node scripts/audit-hu-theme-contrast.mjs` until a wider/topbar-visible browser viewport is used |
| done | Repair shared PI Payments card contrast after theme bridge removal | `PaymentHeroCard` now uses a valid inline CSS fallback gradient plus inset contrast, and `HuKidsPiMenuFrame` supplies neutral native `--pi-payment-hero-*` bridge variables instead of theme-tinted card repainting. `node scripts/audit-hu-theme-contrast.mjs`, build, template audit, platform audit, diff check, and in-app Browser PI/HU + HU Kids smoke passed on 2026-06-13 |
| done | Commit remaining Kids Learn source/reference image assets | Per the user's 2026-06-13 request to commit everything and keep `main` clean, `kids-img/` plus the old generated candidates (`card-confidence.png`, `money-basics.png`, `online-safety.png`, `request-money.png`, `saving-goals.png`) are committed as reference/archive assets. They are not imported by runtime code |
| todo | Decide long-term storage for Kids Learn reference assets | The committed `kids-img/` source/reference set is useful for design iteration but adds about 46.8 MB of image assets. Future cleanup should decide whether these stay in-repo, move to external design storage, or are replaced by final brand-approved artwork |
| done | Reframe HU Kids Home hero as daily spend, not total balance | HU Kids Home now shows `4.500,34 HUF` with `are available for you to spend today`, leaves total holdings in `All your money`, and updates quick actions to `Request money`, `Send money`, `My card`, `More Options`; `My card` opens HU card details. Build and both audits passed on 2026-06-11 |
| todo | Tune Nordlys ember intensity in light mode and decide on theme persistence / premium-locked states | Ember is subtle over the bright dawn zone in light mode; theme state still resets on reload (pre-existing) |
| done | Give each HU theme a distinct premium motion identity | Blue Lines=meridian light blades, Bubbles=parallax bokeh, Aurora=magenta silk curtains, Garden=canopy light + gold pockets, Solar=halo rings; all on the layered motionLayers engine with generic hu-motion-* keyframes; build + audits + light-mode browser smoke passed 2026-06-11 |
| done | Fix theme architecture: atmosphere + glass, not global token repaint | Removed global --uc-* token hijack in getHuThemeStyle; clean native surfaces; translucent glass cards on Home/Analytics scope only; frosted neutral controls/nav; sub-pages native + whisper; preview matches Home. Build + audits + browser smoke (Nordlys dark, Garden light, Payments native) passed 2026-06-11 |
| todo | Visual polish pass on Solar and Bubbles motion in both modes | Redesigned with the shared engine but not individually screenshotted |
| todo | Decide if learn/tutorial cards should also go fully native | Still carry a light themed gradient (--hu-learn-* vars); intentionally left as a self-contained learning surface |
| done | Fix WCAG AA contrast for Blue Lines, Bubbles, Aurora, Garden, Solar | New `--hu-theme-accent-strong` (color-mix accent->text, per-theme P) for links/progress/chips/nav, per-theme heroMuted overrides, Blue Lines dark wash anchored on static-black; verified by `scripts/audit-hu-theme-contrast.mjs` (all pairs >=4.5 text / >=3.0 non-text in both modes), build + audits + browser smoke passed 2026-06-11 |
| done | Restyle HU Kids Bubbles as Blockcraft | Internal `bubbles` theme id is preserved, but the visible theme is now `Blockcraft` with grass/dirt/torch colors, blocky top motion layers, step-based `hu-motion-craft-*` CSS animation utilities, and updated contrast audit fixture. `node scripts/audit-hu-theme-contrast.mjs`, `npm run build`, `npm run audit:templates`, and `npm run audit:platform` passed on 2026-06-11; HU Kids visual click-through remains recommended |
| done | Polish Blockcraft lateral edge blend and cube depth | `HuThemeShell` now applies the theme background directly to the shell, layered motion extends beyond the side edges and fades down, and Blockcraft uses repeated conic-gradient cube tiles via `.hu-motion-craft-cubes-36` for a subtle 3D effect. Contrast audit, build, template audit, and platform audit passed on 2026-06-11; exact light/dark visual approval remains a manual review item |

## Upcoming

| Status | Task | Evidence / Notes |
| --- | --- | --- |
| done | Refactor visible topbar wording from `Version` to `Release` | Runtime state now uses explicit `release` |
| done | Wire `releaseRegistry` into the control panel | `DemoTopBar` and `DemoFeatureSidePanel` use release metadata |
| done | Add product selector for `PI` / `SME` | SME is selectable and renders planned-state placeholder |
| done | Add design-system selector for `current` / `next` | Next DS is selectable and renders planned-state placeholder |
| done | Build initial control panel view for feature lifecycle and coverage | Shows lifecycle/coverage badges from feature metadata |
| done | Add screen/component catalog export for AI training and larger-platform integration | `componentRegistry.ts` and `aiCatalog.ts` |
| done | Update platform capability map after runtime architecture changes | Updated release/control-panel wording |
| done | Add Bosnia Banja Luka country/application variant | `BA_BL` duplicates Bosnia as a separate selectable country/application context across demo state, country/language/translation registries, runtime configs, data fixtures, screen/flow/template registries, Design System country selector, docs, and capability map; build, template audit, static coverage audit, and browser smoke passed on 2026-06-02 |
| done | Add Payments menu for all countries | `PaymentsScreen`, `paymentsMenuConfig`, navigation, screen/component/flow registry entries |
| done | Add Products menu for all countries | `ProductsScreen`, `productsMenuConfig`, navigation, screen/component/flow registry entries; ShopSmart tabs enabled for RO/CZ/SK/HU only |
| done | Add Investments Portfolio screen for eligible PI countries | `InvestmentsPortfolioScreen`, `investmentsPortfolioConfig`, reusable `components/investments/*`, Home `investment_account` card routing, `investmentsAvailability`, navigation/screen/component/flow/project-pack registry entries, and `runtime.investments` fallback translations; applies to Mobile PI Retail in every supported country/application variant including BA/BA_BL. `Total value` is derived from owned `investment_account` products returned by `useProducts()` and country currency conversion. Follow-up on 2026-06-04 made the top tabs interactive: `Performance` keeps the period chart, while `Product Type`, `Currency`, `Asset Class`, and `Account List` render reusable donut/list distributions grouped from the same mock security holdings, including multiple securities accounts and EUR/local/USD/GBP instrument currencies. The Investments tabs now reuse the Design System `MessagesMailboxTabs` component in scrollable mode, so all tab items can be selected by horizontal swipe. Build, template audit, platform audit, diff check, static grouping audit, and in-app browser smoke passed on 2026-06-04, including BA Home -> Investments routing and scrollable/selectable Investments tabs |
| done | Add Analytics / My Spendings tab | `AnalyticsScreen`, navigation wiring, screen/component/flow registry entries; reused shared header actions, New payment banner, Account Detail divider, and AccountTransactionRow; build and in-app browser computed-style smoke passed on `5175` |
| done | Add runtime Messages screen from template 52 | `MessagesScreen`, `messagesConfig`, navigation wiring, header Messages actions, screen/component/flow/template registry entries; build, color audits, `git diff --check`, and in-app browser smoke passed on `5175` |
| done | Add New payment bottom sheet for all countries | `BottomSheet`, `PaymentsScreen`, `paymentsMenuConfig`; verified on `5175`, including CZ domestic subtitle |
| done | Componentize and fix Products menu cards | `ProductMenuCard` is shared by the core product cards; verified `164x120`, `16px` padding, `10px` gap, and `18px` bold white labels on `5175` |
| done | Add homepage hide/show amounts | Global `amountsHidden` state masks account/card/product balances across navigation; transaction amounts remain visible; verified hide/show on `5175` |
| done | Add Account Detail transaction search | `AccountSearchBar` is interactive, filters current-account transactions, swaps Filters for the supplied clear-results icon, and scrolls to the sticky search position on activation; verified on `5175` |
| done | Add Transaction Detail and Domestic payment flow | Transaction rows open Transaction Detail; `Redo payment` prefills Domestic payment; Payments/New payment/Domestic opens a blank form; create/review/sign/success flow is registered and build-verified |
| done | Add Design System Inventory Templates tab | `templateRegistry.ts` represents all 30 files in `screenshots/`; `DesignSystemPage` renders selectable template cards and a selected-preview panel; build and in-app browser smoke passed on `5175` |
| done | Reconstruct first 3 screenshot templates as code previews | `TemplateCodePreviews.tsx` renders template 52, template 67, and Product as real JSX; `templateRegistry.ts` marks their `codePreviewId`; Templates grid is compact with code/source preview toggle; build, raw color audits, `git diff --check`, and in-app browser smoke passed on `5175` |
| done | Reconstruct five feedback status templates as code previews | `TemplateCodePreviews.tsx` now renders Informative, Pending, Success to be, Error to be, and Warning to be through one shared feedback status primitive; `templateRegistry.ts` marks their `codePreviewId`; build, raw color audits, screenshot registry coverage, `git diff --check`, and in-app browser smoke passed on `5175` |
| done | Reconstruct another 10 screenshot templates as code previews | `TemplateCodePreviews.tsx` now renders Sign/PIN, Generate Token, Message detail, New request with push, account-selection panel, Apple Pay, Successful payment, Tutorial intro, Product selection, and RS Travel Insurance as JSX; this was the second reconstruction batch before the final 30/30 pass; build, raw color audits, screenshot registry coverage, `git diff --check`, and in-app browser smoke passed on `5175` |
| done | Reconstruct final 10 source-only screenshot templates as code previews | `TemplateCodePreviews.tsx` now renders Account options, Activate Mobile Token, Analytics, Cards, Contact sheet, account-detail homepage, Domestic payment, Review request, Review data, and Transaction detail as JSX; `templateRegistry.ts` now reports 30/30 code-backed templates; `npm run build` passed |
| done | Add 10 code-only active-pattern templates | `TemplateCodePreviews.tsx` now renders Home dashboard, Payments menu, New payment sheet, Products menu, More menu, Contacts directory, Account details info, Messages outbox, Prime advisor, and Prime benefits as reusable code-only templates; `templateRegistry.ts` reports 40 code-backed templates total: 30 screenshot-backed and 10 code-only; build, static coverage, `git diff --check`, and in-app browser smoke passed on `5175` |
| done | Add another 10 code-only active-pattern templates | `TemplateCodePreviews.tsx` now renders Prelogin inactive, Prelogin active, Language selector sheet, Other panel menu, Co-Apping session, Account transactions list, Account search results, Spending money out, Products ShopSmart, and Logout confirmation as reusable code-only templates; `templateRegistry.ts` reports 50 code-backed templates total: 30 screenshot-backed and 20 code-only; build, static coverage, raw color audits, `git diff --check`, and in-app browser smoke passed on 2026-06-01 |
| done | Add typed template-to-app contract and audit | `templateRegistry.ts` now treats every template as a standalone page/state pattern with typed `ComponentId`, `ScreenId`, `FlowId`, screen family, runtime screen, AI reuse rules, and do-not-invent rules; `componentRegistry.ts` now includes the missing DS primitives used by templates; `npm run audit:templates` validates all 50 templates against the source registries; build passed on 2026-06-02 |
| done | Componentize and fix Products offer cards | `ProductOfferCard` renders the Products offer carousel card at `327x157`, with a `206px` text stack, `8px` gap, `24px` bold title, and `16px` regular subtitle; build and in-app browser computed-style verification passed on `5175` |
| done | Add centralized AppIcon registry and Design System Icons tab | `AppIcon.tsx` is the single reusable UI icon repository; app icon consumers were routed through `AppIcon`; `DesignSystemPage` renders the `Icons` tab with mapped app icon cards, centralized lucide-alone wrappers, audit boundaries, and the HU Kids quick-action glyphs; build and in-app browser smoke passed |
| done | Remove redundant lucide wrappers with custom equivalents | `AppIcon.tsx` removed lucide `share-2`, `copy`, `check`, `bell`, `file-text`, `qr-code`, `chevron-down-lucide`, `plus-circle`, `filter`, and lucide `close-x`; active/template usage now maps to custom registry icons, and remaining lucide-alone keys are explicitly centralized; build, icon audits, template coverage, `git diff --check`, and browser Icons-tab smoke passed on 2026-06-01 |
| done | Fix Products offer carousel drag/snap and Products spacing | Offers carousel now supports Account Detail-style drag/snap; Products page spacing measured at `16px` for tab/offers/carousel/products/grid gaps; build and in-app browser smoke passed on `5175` |
| done | Add Design System Colors tab and Light/Dark appearance mode | `colorRegistry.ts` maps `screenshots/Colors.svg` into DS palettes and app color audit entries; `theme.css`, `demoStore`, `DemoTopBar`, `DemoFeatureSidePanel`, and `DesignSystemPage` implement color inventory, copy feedback, and Light/Dark switching; build, browser smoke, raw hex/rgb, and Tailwind palette audits passed on `5175` |
| done | Retire RO Kids Banking prototype for Mobile PI Kids Romania | On 2026-07-07 the RO Kids runtime app and `roKidsBanking` data were deleted rather than hidden. RO Kids now falls back to the planned-state placeholder through normal app gating and project-pack metadata. |
| done | Keep SK/HU Kids concepts active and retire RS runtime | `KidsMarketHomeApp` and `kidsMarketHomeConcepts` now render current-design-system Mobile PI Kids concepts only for SK/HU. Serbia Kids safe-spend coach files, data, style hooks, registries, and flow entries were deleted on 2026-07-07. |
| done | Refactor Slovakia Kids from Bulbank Teen/Kids document | Parsed and visually inspected `UniCredit Bulbank teen(kids) mode 5.2.pdf`; SK now uses a Bulbank-inspired Products home with Accounts, Shortcuts, Cards, Offer, plus dedicated Education, Tasks, and More pages. `npm run build`, `npm run audit:templates`, `npm run audit:platform`, raw Kids color scan, and in-app browser SK Home/Education/Tasks/More smoke passed on 2026-06-04 |
| done | Add global interactive cursor affordance | `src/styles/theme.css` applies pointer cursors to clickable controls across runtime screens, templates, and Design System specimens, preserves text cursor for text fields, and uses `not-allowed` for disabled controls; `npm run build`, browser computed-cursor checks, and closeout verification passed |
| done | Add Payments hero-card image variants and per-card sheet contract | `PaymentHeroCard` supports 9 screenshot-backed artwork variants from `screenshots/payments1.png` through `screenshots/payments9.png`; `paymentsMenuConfig` now has per-card `heroSheets`; Romania and BA/BA_BL have country-specific primary/shortcut copy, while remaining country/card applicability and overlay copy remain future work |
| done | Align BA and BA_BL More, Products, and Payments runtime menus | BA/BA_BL More now uses Contacts, Documents, Settings, Tutorials, My applications plus Contact phone + Messages header actions; Products are cards-only with Accounts, Cards, Loans, Savings; Payments has five primary cards and five OTHER shortcuts; build and browser smoke passed on 2026-06-02 |
| done | Make More Documents badge dynamic | `MoreScreen` now uses `getDocumentsCountForCountry`; every supported country currently resolves to 7 document rows; build, template audit, static config audit, and browser smoke passed on 2026-06-02 |
| done | Align Contacts header and section dividers with shared components | `ContactsScreen` now uses `PageHeader` collapsed-title progress and `SectionHeadingDivider`; Design System specimen/registry/docs updated; build, template audit, diff check, and browser smoke passed on 2026-06-02 |
| done | Add runtime phone screenshot export control | `PhoneScreenshotControl` now renders in the demo top bar immediately after Settings / Control Panel as an icon-only dropdown and exports visible/full-height PNGs through `phoneScreenshot.ts`; full-height captures re-anchor L1 bottom navigation to the bottom of the exported PNG; Design System Inventory keeps the control visible but disabled; build, template audit, diff check, and browser/CDP smoke passed on 2026-06-02 |
| done | Add code-derived Figma JSON export options | The demo camera dropdown now includes `Generate visible JSON` and `Generate entire screen JSON`; both copy Figma-ready `build-ui.screen.v1` layer-tree payloads from an unscaled clone of the rendered phone content, not from a screenshot image. The payload uses app-content background instead of phone-shell black, numeric absolute bounds, Figma paints/effects/text specs, `assets[]`/`assetRef`, text safety width, designer-friendlier fallback names, conservative Auto Layout `layout` / `autoLayoutChild` intent, app-side generated JSON validation, top-level quality warnings where needed, and source metadata for visible/full export mode. JSON delivery now uses async `ClipboardItem` writes and falls back to `.json` download if clipboard permissions remain blocked. Build, plugin syntax check, audit bridge app-exporter static check, and browser JSON smoke passed on 2026-06-03 |
| done | Add bidirectional Component-E / UniCredit Build UI Bridge plugin | `figma-plugins/screen-json-importer` is now the clean local bridge source, and `screenshots/FIgma plugins/Component-E` has been synced into the same single bidirectional plugin as `Component-E Build UI Bridge` while preserving the original Component-E development plugin id. `Build from JSON` imports `build-ui.screen.v1` into editable Figma layers and also normalizes legacy Build-UI / Component-E-compatible JSON (`codex-figma-component-spec/v1`, `components[].root`, `roots[]`, `root`, `screen`, `children[]`, `layers[]`, and inline SVG/PNG/JPEG `asset.dataUrl` / `dataUrl` assets); `Extract selection` exports selected Figma frames/components/groups/layers back into the same schema with assets, Figma styles, root frame styling, Auto Layout, text segments, rotation, visibility, locked state, warnings, source metadata, safe wrapping for single-layer selections, `components[]` / `variantSets[]` companions, raw style refs, component props, bound variables, and optional PNG 2x snapshots. Build import now runs preflight diagnostics before creating Figma nodes, blocking invalid canonical geometry and surfacing likely fidelity issues as warnings; the English UI also has a Diagnostics panel that lists preflight stats, all build/extract warnings, companion component/variant counts, and optional PNG snapshot refs. UI, README, and manual smoke fixture docs are English-only; `npm run audit:figma-bridge` is now the repeatable static + VM smoke gate for both bridge copies and app-side exporter static checks, imports the shipped canonical/legacy manual smoke fixtures, guards the Diagnostics panel, and covers extracted-JSON re-import round trips plus preflight error/warning cases. `node --check`, Component-E `npm run build`, Component-E `npm run lint`, manifest parse, VM bridge smoke for import/build/export paths, `npm run build`, `npm run audit:templates`, `npm run audit:platform`, static bridge audit, plugin whitespace audit, UI script parse checks, relevant `git diff --check`, English-only plugin UI scan, and browser JSON UI smoke passed on 2026-06-03. Manual runtime smoke inside Figma remains a follow-up because the Figma plugin API is not available in Node/browser verification |
| done | Componentize and resize Payments OTHER shortcut bubbles | `PaymentOtherShortcutIconBubble` now owns the 8px-padding hug bubble with a centered 32x32 icon slot; `PaymentOtherShortcut` labels now use 14px UniCredit bold centered normal line-height without letter spacing; build, template audit, diff check, and browser smoke passed on 2026-06-02 |
| todo | Add automated regression for Investments distribution donut geometry | `InvestmentDistributionChart` now anchors each connector at the real slice midpoint (no hard-coded 4-slice branch) and caps the donut to 4 visible slices. Add a committed test that verifies connector side/color matches slice midpoint for Product Type / Currency / Asset Class / Account List and that the 5-row Product Type distribution still renders 4 donut slices + 5 list rows. Manual numeric check done; no committed regression yet. |
| done | Add automated regression for Card Details carousel shadow + action boundaries | `card-detail-boundaries.test.tsx` now guards the raised 20px carousel shadow lane, direct non-sensitive `Card Details`, and Face-ID-gated `Show Card Details` sensitive reveal. Focused tests and live port-4001 evidence are recorded in `current-session.md`. |
| todo | Add automated regression for Tools side-by-side header + capacity | The Tools / Side-by-Side `Reload frames` text button is now a 32×32 refresh icon button aligned to the panel's right edge, and country capacity dropped from 2–4 to 2–3. No committed test guards the new header layout, the right-edge alignment, the icon button's `aria-label`, or the `MAX_COUNTRIES = 3` cap. |
| done | Add automated regression for demo topbar App+Country selector | Final selector uses a compact Radix root menu plus adjacent country submenu, matching the More menu's visual atoms while remaining intentionally narrower. `tests/components/demo-top-bar.test.tsx` verifies that the three-app root stays visible while the country submenu opens; browser smoke verified selection and More-menu visual parity. |
| todo | Add automated regression for Investments order-documents accordion visual contract | The "DOCUMENTS AND TERMS" accordion rows now use a 16px Bold title (`uc-type-n4-strong`), a narrower `chevron-down` (instead of `chevron-down-wide`), an Ex-Ante glyph that keeps its native 15:20 aspect ratio via `iconWidth={24} iconHeight={32}`, a leading icon that recolors to `--uc-action` when the row is open, 24px-inset row separators matching the `SectionHeadingDivider` heading line, and 24px-inset inner group separators inside the cost breakdown blocks. No committed test guards the icon aspect ratio, title size, separator inset alignment, or open-state recolor. |
| todo | Wire HU Kids Goal Detail Withdrawal and Settings actions | The Goal Detail action rail (under "Saved so far") has 3 buttons. Add Money now opens a full-screen Revolut-style Add-money screen (calculator keypad + account picker) and applies via `handleAddGoalMoney`. Withdrawal and Settings remain no-op placeholders that reuse the `account-options` gear glyph because no dedicated icons exist. They should get real flows + dedicated icons when product scope is confirmed. |
| todo | Replace HU Kids mock accounts and add focused goal-funding regression | The Add-money account list (`HU_KIDS_ACCOUNTS`) is hardcoded rather than sourced from `useProducts`; entered amounts are balance-checked, but the complete account-picker/calculator/submit path has no focused automated regression. The arithmetic evaluator is intentionally small (×÷ then +−, no parentheses/exponent). When product scope is confirmed, wire real account data and decide whether to retain the bounded evaluator or adopt a vetted utility. |

## Future Product Work

| Status | Task | Evidence / Notes |
| --- | --- | --- |
| todo | Import or build actual SME screens | Selector exists; runtime placeholder prevents false coverage |
| todo | Implement actual next design-system screens/components | Selector exists; runtime placeholder prevents false coverage |
| todo | Replace Phase 1 mock banking repositories with API adapters when backend contracts exist | `bankingRepositories.ts` is adapter-ready but currently reads scenario mocks only |
| todo | Wire effective app context into more runtime screens | Payments primary cards now respect disabled payment actions; Home, Products, Cards-like future surfaces, Documents, and Kids/SME future screens should consume the same context intentionally |
| todo | Add release-promotion workflow UI after stakeholder approval | Source-level baseline ledger and readiness checks exist, but there is no persisted publication workflow or audit history |
| todo | Define a repeatable Figma JSON-to-component import pipeline | The first `Card` import was implemented manually from the plugin JSON; future imports should standardize asset extraction, component naming, registry IDs, DS specimen generation, and audit expectations |
| todo | Add component-aware Figma JSON V2 mapping | The current Build UI Bridge is bidirectional and generic: app DOM -> `build-ui.screen.v1` -> editable Figma layers, and Figma selection -> the same schema. Pixel-perfect Figma reconstruction should next map known runtime components, UniCredit fonts, tokens, clipping, shadows, masks, semantic sections/cards/rows, grouped money typography, and reusable DS assets explicitly instead of relying only on generic geometry/style serialization |
| todo | Audit and polish active SK/HU Kids journeys | RO/RS Kids apps were deleted on 2026-07-07. Next Kids product work should inspect only the active SK/HU concepts unless a new country concept is explicitly approved. |
| todo | Deepen selected SK/HU Kids market concepts after comparison | SK and HU remain the active mock-driven Kids concept variants. HU has begun a supplied-reference CEE Light Restyle pass, but both active concepts still need interaction polish and full child/parent flow depth before any unified Kids direction is chosen. |
| todo | Polish HU Kids CEE Light Restyle interactions | Current HU page now has a close static/mock homepage port plus theme personalization from More Options. Next pass should refine theme artwork/names, decide whether selected theme persists across reloads, cut/replace remaining static elements against the supplied screenshot/JSON, and decide which actions become interactive |
| todo | Unify the best active Kids concepts after the country battles | Do not prematurely generalize; choose strongest patterns from SK/HU after separate executions and follow-up polish. Add any retired/new market back only through an explicit product decision. |
| todo | Add automated smoke coverage for active Kids flows | Add committed regression coverage for SK Products/Education/Tasks/More and HU request/send/goals/learn/theme/card-details flows. |
| in_progress | Add automated smoke coverage for SK Bulbank Kids tabs | 2026-07-18: `tests/screens/sk-kids-tree.test.tsx` now guards SK Home (account, card, offer) plus the Education, Tasks, and More tabs after the `sk/` extraction. The no-Payments Kids nav rule is still unguarded. |
| todo | Add tests for product/release/design-system switching | Build passes, but no automated UI tests yet |
| todo | Add visual regression coverage for account-detail safe-area, title-card spacing, sticky search, account-card metadata/spacing, account carousel edge-peek/drag/snap, all-products carousel coverage, account-details info screen layout, and desktop preview auto-fit behavior | Manual browser verification caught the Dynamic Island overlap regression, page-level scroll regression, account-card metadata/spacing regression, sticky-search spacing regression, account-card carousel gutter/drag/edge-peek regression, all-products account carousel coverage, and Account Details info navigation; no automated guard exists yet |
| todo | Map Payments hero-card variants, overlay menus, and final copy per remaining country | `PaymentHeroCard` now supports 9 screenshot-backed image variants and each primary card has a dedicated `heroSheets` overlay config; Romania and BA/BA_BL have requested primary/shortcut copy, while final mapping/copy for CZ, SK, HU, RS, and SI plus overlay menu labels and dedicated shortcut icons are pending user-provided mapping |
| todo | Localize and approve Mobile PI Tutorials content per market | More -> Tutorials now exists for all Mobile PI countries with mock tutorial pages; final legal/product wording, market-specific tutorial availability, and real tutorial media/assets remain content/product follow-up work |
| todo | Replace remaining shared Payments placeholder labels with country-specific copy | Current Payments config is country-scoped; Romania and BA/BA_BL have explicit requested copy, while CZ, SK, HU, RS, and SI still reuse the shared baseline English labels |
| todo | Fine tune Products copy, imagery, ShopSmart content, and per-country labels | Current Products config is country-scoped and the core card component contract is fixed; copy/assets are still shared placeholders |
| todo | Replace shared Products card bottom-sheet option lists with per-country specs | Products cards now open a standard bottom sheet in every Mobile PI country using shared mock values; update `productsMenuConfig.ts` once final country/product option lists, translations, and destination flows are supplied |
| todo | Add automated tests for Investments Portfolio routing, total-value calculation, tab distributions, and all-country routing | Manual in-app browser smoke passed on 2026-06-04 for RO Home -> Investments, `9.166,97 RON` total-value derivation from the owned investment product, sorting, and Back; static grouping audit passed for Product Type/Currency/Asset Class/Account List distributions summing to 100%. No committed regression test guards routing for every PI country/application variant, amount masking, tab switching, distribution grouping, sorting, or accordion state yet |
| todo | Fine tune Analytics chart spacing and PFM aggregation behavior | Current Analytics screen is screenshot-inspired and transaction-derived from static account profiles; Money Out / Money In PFM categories are wired, but chart interaction, exact spacing, and product-specific transaction profile depth still need final tuning |
| todo | Extract remaining PFM category glyphs from `screenshots/PFM-icons.svg` | Taxes and Penalties, Income, Home, Utilities, Transportation, Children, Healthcare, Shopping, Lifestyle, Education, Leisure time, Investments, Uncategorized, Groceries, Exclude from budget, Insurance, Finance, Wallet, and Transfers now render real 20x20 SVG glyphs; remaining categories such as Cash still use token-colored initial badges |
| todo | Add automated tests for Messages header routing and list interactions | Build and in-app browser smoke passed for RO and CZ; no automated regression test exists yet for header Messages entry, Back navigation, Inbox/Outbox switching, or search filtering |
| todo | Add automated tests for BA/BA_BL menu differences, Documents badge count, and Contacts header collapse | Build and browser smoke passed manually on 2026-06-02, but no automated regression test guards BA/BA_BL More/Products/Payments labels, the Documents counter, or Contacts `PageHeader` collapse/`SectionHeadingDivider` usage |
| todo | Add automated tests for the Vercel access gate | Password gate now has middleware/API/local-dev smoke coverage from this session, but no committed regression test guards `/access.html`, `/api/access`, signed-cookie validation, 6-month/1-month durations, 10-attempt temporary blocking, or the no-cookie-reset copy rule |
| todo | Add automated coverage for runtime phone screenshot, JSON export, and Build UI Bridge | Browser smoke confirms the top-bar screenshot control/dropdown, visible PNG download, Design System disabled state, L1 full-height bottom-navigation anchoring, visible/full-height JSON modes, async clipboard JSON delivery, and no new console errors; add committed browser/plugin tests that validate visible/full-height PNG dimensions, bottom-nav placement, header placement, disabled Design System state, JSON schema/mode/layer presence, file download/clipboard behavior, and a manual or automated Figma plugin import/export smoke for `UniCredit Build UI Bridge` / `Component-E Build UI Bridge` |
| todo | Fine tune Domestic payment flow screens | Mock flow exists from both New payment and Redo payment, but needs screenshot-level copy/spacing/country refinements |
| todo | Implement remaining New payment action flows | Domestic is implemented as a mock flow; Foreign/SEPA and Templates/Beneficiaries remain placeholders until target screens are supplied |
| todo | Add automated tests for hide/show amounts | Build and Chrome smoke passed; no automated regression test exists yet for account/card masking and transaction exclusion |
| todo | Add automated tests for Account Detail transaction search | Build and in-app browser smoke passed; no automated regression test exists yet for search filtering, clear reset, and activation scroll |
| todo | Add automated tests for Transaction Detail and Domestic payment navigation | Build and in-app browser smoke passed; no automated regression test exists yet for New payment -> Domestic and Transaction Detail -> Redo payment paths |
| todo | Add automated visual tests for Design System template selection | `npm run audit:templates` now guards semantic registry drift, but no browser regression test yet verifies card selection, source/code toggle behavior, or visual rendering of every template preview |
| done | Reconstruct remaining source-only screenshot templates as reusable code previews | Current screenshot-backed coverage is 30/30, and the Design System Templates registry now has 50 code-backed previews after adding 20 code-only active-pattern templates |
| todo | Add automated visual tests for reconstructed template previews | Semantic contract audit now passes for all 50 code previews; browser smoke confirmed 50 cards, default `code` mode, source toggle only for screenshot-backed templates, compact `168px` cards, and selection for reconstructed/code-only template packs, but no automated visual regression suite exists yet |
| todo | Add automated visual/style coverage for Products offer cards | Build and in-app browser smoke passed; no automated regression test exists yet for the `327x157` offer-card contract, Products `16px` spacing rhythm, or offer carousel drag/snap behavior |
| todo | Add automated icon registry audit | Manual icon cleanup passed after removing redundant lucide wrappers and centralizing active app icons; no automated regression test exists yet to fail when a reusable app icon is added as raw inline SVG or direct `lucide-react` import outside `AppIcon` / vendored `ui/**` |
| todo | Add automated color-token audit | Manual `rg` audits passed, but no automated regression test fails if raw app hex/rgb/Tailwind palette classes are reintroduced outside `colorRegistry.ts` / asset boundaries |
| todo | Add automated typography-token audit and finish non-canonical runtime exceptions | Canonical typography tokens now live in `typographyRegistry.ts` and `theme.css`, and major non-Kids runtime/shared surfaces were migrated on 2026-06-03. Remaining explicit sizes are concentrated in intentional taxonomy gaps such as `38px`, `26px`, `22px`, `13px`, and `12px`; add a regression audit that flags new raw UniCredit sizes where a canonical token exists, then decide whether the non-canonical gaps need extra named tokens or should remain explicit |
| todo | Add dark-mode visual regression coverage | Light/Dark browser smoke passed for the Colors inventory and theme root vars, but no visual regression suite covers every screen in both themes yet |
| todo | Continue full app localization migration | More screen card labels are now translation-backed across all country/language files; remaining active screens still need a hardcoded-copy audit and translation-key migration |
| todo | Add automated cursor-affordance regression coverage | Global CSS now covers clickable controls, but there is no automated UI test yet that verifies transaction rows, cards, tabs, and disabled controls keep the expected computed cursors |
| done | Repair More Tutorials detail bottom-sheet header | Detail sheet now starts below the phone status bar via the safe-area reserve and uses the shared 20px `back-heavy` glyph; build, audits, diff check, and browser smoke passed. |
