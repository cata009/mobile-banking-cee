/**
 * Capture geometry and selectors shared by the screenshot and Figma paths.
 *
 * Extracted verbatim from phoneScreenshot.ts.
 */

export const SCREEN_WIDTH_FALLBACK = 375;

export const SCREEN_HEIGHT_FALLBACK = 812;

export const CANVAS_SCALE = 1;

export const BOTTOM_NAVIGATION_SELECTOR = "[data-phone-bottom-navigation='true']";

export const DEVICE_SHELL_BACKGROUND_HEXES = new Set(["#000000", "#1F1F1F", "#262626"]);
